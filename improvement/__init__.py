# -*- coding: utf-8 -*-
"""
지속 개선 엔진 — 매 거래일 케이스 자동 축적 · 결과 추적 · 모델 버전 관리.

원칙 (사용자 지시문 그대로):
  1. 운영 모델은 자동으로 변경하지 않는다 — 후보(candidate)로만 생성.
  2. 승격은 블라인드 표본·적중률·기대수익·PF·MDD·신호율 조건 전부 통과 시에만.
  3. 같은 종목·같은 날짜·같은 모델 중복 케이스 금지 (case_id 해시 + INSERT OR IGNORE).
  4. 목표가·손절가가 같은 날 도달하면 임의로 성공 처리하지 않는다 (unresolved).
     ⚠️ 이 정의는 improvement DB(실전 추천 추적) 전용이다 — 기존 리플레이
     KPI(보수적 손절 우선)는 정의 불변 원칙에 따라 그대로 둔다. 두 수치는
     정의가 다르므로 화면에서 섞지 않는다.
  5. 추천 당시 입력·점수·가격·버전·데이터 해시를 동결(append-only).
  6. 데이터 오류와 모델 실패를 별도 분류(data_error vs failure).
"""
