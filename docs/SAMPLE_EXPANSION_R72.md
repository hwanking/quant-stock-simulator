# 표본 확장 — 라운드 72

**"60,462건이 적은가?"** 에 답하려면 먼저 그 60,462건이 무엇인지 세야 한다.

```bash
C:/Python314/python.exe scripts/sample_audit.py                          # 정보량 감사
C:/Python314/python.exe scripts/calibration_lab.py --universe 1500 --shard 1/6 --limit 25000
C:/Python314/python.exe scripts/calibration_lab.py                       # 전 샤드 합산 채점
```

---

## 1. 먼저 잰 것 — raw 가 아니라 정보량

| 지표 | 값 |
|---|---:|
| ① raw cases | 60,462 |
| ② 고유 종목 | 583 |
| ③ 고유 거래일 | 1,728 |
| ④ **독립 에피소드** (35일 묶음) | **17,428** (raw의 29%) |
| ⑤ 섹터군집 보정 유효표본 | 3,476 (섹터 미기록 54,718건 = 90.5%) |
| ⑥ 시장국면 보정 유효표본 | 1,831 |
| ⑦ 전방 전용 평가 n | **0** |
| ⑧ **고신뢰 전방 신호 n** | **0** |

기간은 **2013-11-12 ~ 2026-07-06** 이지만 실질은 2018년 이후다.
2013~2017 을 다 합쳐 **172건**뿐이고 2018년부터 연 4,000~8,200건이다.

**왜 앞이 비었나.** 생성기가 종목당 `n_dates=80` 개를 `spacing=25` 거래일
간격으로 뽑는다. 80 × 25 = 2,000봉이라 최근에서 과거로 8년만 닿는다.

국면은 **40.8%가 미기록**(`None`)이고, 섹터는 **90.5%가 미기록**이다.
섹터별 표본을 논하려면 이것부터 채워야 한다.

---

## 2. 천장을 먼저 쟀다

넓히기 전에 **얼마나 넓힐 수 있는지**부터 실측했다. 목표 숫자를 정해 놓고
거기에 맞추면 중복을 만들어서라도 채우게 된다.

### 날짜 축 — 천장 108개/종목

`start_date` 를 2010 으로 당겨도 **소용없다.** 시세 제공처가 종목당 약
3,000봉만 준다(2014-05-20 부터). 앞 260봉(지표 워밍업)과 뒤 21봉(채점용)을
빼면 usable 2,716봉이고, 간격 25로 뽑으면 `2,716 // 25 = 108` 개가 전부다.

현재 80 은 그 천장의 **74%** 만 쓰고 있었다. → `N_DATES = 108` 로 올렸다.

**간격(25)은 건드리지 않았다.** 보유기간이 20영업일이므로 그보다 좁히면
같은 종목의 인접 케이스가 겹쳐 독립성이 깨진다. 표본을 늘리려고 중복을
만드는 것은 raw 만 부풀리고 정보량은 그대로다.

### 종목 축 — 천장 3,014개

| | 종목 | 계획 최대 | 신규 |
|---|---:|---:|---:|
| 현재 (고정 목록) | 600 | 64,800 | — |
| `--universe 600` | 835 | 90,180 | 29,718 |
| `--universe 1000` | 1,141 | 123,228 | 62,766 |
| **`--universe 1500`** | **1,582** | **170,856** | **110,394** |
| `--universe 3014` | 3,066 | 331,128 | 270,666 |

전 종목은 3,014개(KOSPI 1,421 · KOSDAQ 1,593)다. `--universe 1500` 을
택했다 — 시총 1,159억 이상이고, 계획 170,856건이다.

---

## 3. 붙인 것

- **`N_DATES = 108`** — 데이터 천장까지. 2015~2017 커버가 열린다
- **`--universe N`** — 시총 상위 N 까지 유니버스 확장
- **`--shard i/n`** — 종목을 n 갈래로 나눠 병렬 축적. 워커는 자기 샤드
  파일에만 append 하고 **채점하지 않는다** (채점은 원장을 통째로 다시
  쓰므로 동시에 하면 서로를 덮는다 — 라운드 71c 와 같은 사고)
- **유니버스 캐시** — 목록을 파일에 박제한다. 샤드 6개가 각자 3,014종목을
  받아 오면 느릴 뿐 아니라 **분할이 깨진다**: 시총 순위는 장중에 바뀌므로
  워커마다 목록 순서가 다르면 `k % n` 이 같은 종목을 두 번 돌리거나
  빠뜨린다
- **`scripts/sample_audit.py`** — 위 8개 지표를 항상 같이 낸다

---

## 4. 정직하게 남겨 둘 한계

**생존편향은 넓혀도 안 없어진다.** 유니버스는 *현재 상장된* 종목만 준다.
상장폐지·거래정지 종목이 없다. 이건 데이터 출처의 한계이고, 종목을
3,014개로 늘려도 그대로다. 성과가 실제보다 좋게 보일 수 있다.

**2014-05 이전은 못 본다.** 3,000봉 상한 때문이다. 2008 금융위기,
2011 유럽 재정위기 같은 큰 하락 국면이 표본에 없다.

**표본을 늘려도 우위가 생기지는 않는다.** 라운드 71c 에서 잰 것 —
이 원장의 비용 후 손익분기 적중률은 63.3% 인데 blind 실적중은 51.86%
(−11.46%p 아래)다. raw 를 3배로 늘리면 **추정이 정밀해질 뿐** 그 격차는
줄지 않는다. 넓히기의 진짜 이득은 다른 데 있다:

- **유사사례 0건 문제 완화** — 횡단면 짝을 찾을 종목이 늘어난다
  (BGF리테일에서 실제로 0건이 나왔다)
- **섹터·국면 빈칸 채우기** — 지금은 섹터가 90.5% 미기록이다
- **중소형주 커버** — 현재 600종목은 대형주 편중이다

**⑧ 고신뢰 전방 신호가 0 이라는 사실은 넓혀도 안 바뀐다.** 그건 시간이
쌓아야 하는 숫자다. 과거 케이스가 30만 건이어도 미래에서 처음 본 고신뢰
추천이 몇십 건뿐이면 그 적중률은 여전히 불안하다.

---

## 5. 실행 결과 (2026-08-11)

6샤드로 축적했고 전 샤드를 합쳐 채점했다. **신규 121,497건.**

| 지표 | 전 | 후 | 배수 |
|---|---:|---:|---:|
| ① raw cases | 60,462 | **181,959** | 3.01× |
| ② 고유 종목 | 583 | **1,547** | 2.65× |
| ③ 고유 거래일 | 1,728 | **2,658** | 1.54× |
| ④ 독립 에피소드 | 17,428 | **57,730** | 3.31× |
| ⑤ 섹터군집 보정 | 3,476 | 3,476 | **1.00×** |
| ⑥ 시장국면 보정 | 1,831 | **2,903** | 1.59× |
| ⑦ 전방 전용 평가 | 0 | 0 | — |
| ⑧ 고신뢰 전방 신호 | 0 | 0 | — |

기간이 **2013-11 → 2011-01** 로 넓어졌고, 비어 있던 2015~2017 이
11건에서 **21,369건**으로 찼다. 종목당 중앙 건수는 80 → 108 로 의도대로 올랐다.

### 표본을 3배로 늘려도 우위는 생기지 않았다

넓히기 전에 예고한 그대로다. 매수권 표본이 **4.2배** 늘었는데 결론은 같다:

| 구간 | 전 n | 전 적중 | **후 n** | **후 적중** | 비용 후 손익분기(63.5%) 대비 |
|---|---:|---:|---:|---:|---:|
| train | 13,796 | 59.11% | **65,858** | 59.51% | **−4.01%p 아래** |
| valid | 1,536 | 66.15% | **6,451** | 68.78% | +5.26%p 위 |
| blind | 1,207 | 51.86% | **5,071** | 53.87% | **−9.64%p 아래** |

blind 가 −11.46%p 에서 −9.64%p 로 조금 올라왔지만 **여전히 선 아래**다.
손익분기선 위인 구간은 valid 하나뿐 — 모델이 맞춰진 그 구간이다.

달라진 것은 **확신의 크기**다. 전에는 blind n=1,207 로 말하던 것을 이제
n=5,071 로 말한다. 표본이 늘어 추정이 단단해졌을 뿐, 우위가 생기지는 않았다.
정합은 181,959건 전건에서 여전히 위반 0건이다.

### 아직 안 채워진 두 칸

- **섹터가 96.8% 미기록** (176,215건). ⑤가 3,476 에서 한 발도 못 움직였다.
  하위점수 패치가 새 케이스를 덮지 않는다 — 섹터별 표본을 논하려면 이걸
  먼저 돌려야 한다.
- **국면이 47.6% 미기록** 으로 오히려 **나빠졌다** (40.8% → 47.6%).
  새로 넣은 종목·기간에서 `regime_at` 이 더 자주 None 을 돌려준다.

둘 다 다음 라운드 몫이다. 지금 상태로는 "섹터별 최소 표본 300~500" 같은
목표를 말할 수 없다 — **셀 수가 없기 때문이다.**

---

## 6. 라운드 73 — 파생층 백필과, 측정 도구가 만든 가짜 구멍

원장만 3배가 되고 파생층은 그대로였다. 채웠다.

| 파생층 | 전 | 후 |
|---|---:|---:|
| 경로 (bar_paths) | 60,462 | **181,959 (100%)** |
| 진입 기준선 | 60,462 | **181,959 (100%)** |
| 돌파 플래그 | 54,961 | **163,505** (잴 수 있는 것 전부) |
| 국면 미기록 | 47.6% | **0.03% (56건)** |

셋은 15분, 국면은 즉시. **섹터는 애초에 백필할 게 없었다.**

### 12시간짜리 작업이 실은 0이었다 — 측정 도구가 틀렸다

`sample_audit` 의 ⑤(섹터군집)가 `subscore_patch` 파일**만** 봤다. 그런데
라운드 72 확장분 121,497건은 축적할 때 섹터를 **원장 행에 직접** 쓴다 —
패치 파일이 애초에 필요 없다. 패치는 그 전 60,462건용이다.

| | 잘못 보고 | 실제 |
|---|---:|---:|
| ⑤ 섹터군집 보정 | 3,476 | **21,987** (6.3배) |
| 섹터 미기록 | 96.8% | 52.9% |
| 남은 작업 시간 | 12시간 | **0** |

**측정 도구가 한쪽만 보면 없는 구멍을 만들어 낸다.** 두 곳을 다 읽도록
고쳤다. 이 라운드에서 두 번째로 겪은 같은 종류의 착오다 — 돌파 커버리지
89.9% 도 "미완" 이 아니라 blind·OPEN 을 뺀 정상값이었다.

### 국면 47.6% 미기록은 지수 봉 수였다

연도별로 가르니 **2021-12-08 이전 100% · 이후 0%** — 순수한 날짜 절단.
`regime_at()` 이 지수를 1,200봉(≈4.8년)만 받았다. 지수도 3,000봉 천장까지
나오므로 거기까지 받도록 고치고, 이미 쌓인 **원본** 86,481건을 채웠다.
산출물이 아니라 원본을 고쳐야 한다 — 산출물은 채점 때 다시 만들어진다.

### 아직 못 채운 것 — 코스닥은 섹터가 통째로 없다

| | KOSPI | KOSDAQ |
|---|---:|---:|
| 섹터 전부 있음 | 287 | **0** |
| 섹터 전무 | 333 | 636 |

업종 정규식 자체는 멀쩡하다 — 코스닥 페이지를 직접 조회해 매칭되는 것을
확인했다. 파이프라인이 그 값을 안 싣는다. `bitemporal_engine` 은 계산
파일이므로 다음 라운드에서 조심해서 본다.

**이것이 채워지기 전에는 "섹터별 최소 표본" 목표를 말할 수 없다** —
코스닥 전체가 분모에서 빠지기 때문이다.

### 표본을 3배로 늘린 뒤에도 결론은 그대로다

최종 원장 181,959건 · 정합 위반 0건 · 손익분기(비용 후) 63.5%:

| 구간 | n | 적중 | 손익분기 대비 |
|---|---:|---:|---:|
| train | 65,858 | 59.51% | **−4.01%p 아래** |
| valid | 6,451 | 68.78% | +5.26%p 위 |
| blind | 5,071 | 53.87% | **−9.64%p 아래** |

---

## 7. 라운드 74 — 섹터가 통째로 비어 있던 이유 세 가지

⑤ 섹터군집이 안 움직인 원인을 끝까지 팠다. **결함이 셋이었다.**

### ① 접미사를 잃고 기본 .KS 로 저장 (코스닥 0%)

`bitemporal_engine.py:706` 이 `fetch_and_update_naver_realtime(code)` 를
접미사 없이 불렀다. 접미사가 없으면 기본이 `.KS` 라 코스닥 종목 정보가
`035760.KS` 에 저장되고 `.KQ` 는 비었다. 읽는 쪽은 `.KQ` 를 먼저 보므로
유니버스 시드가 만든 **섹터 없는 낡은 항목**이 이겼다.

`:491-495` 주석이 시세에 대해 경고한 그 결함이다(에코프로 358,500 vs
79,100). 조회 경로는 고쳤는데 이 호출부가 남아 있었다.
→ 실측 코스닥 **0/10 → 9/10**

### ② 적정가를 못 내면 업종까지 사라짐

`fair_value_status == OUT_OF_DOMAIN` 조기 반환 dict 에 `sector` 가
없었다. 천일고속(PBR 29.56 · ROE −72.99)은 DB 에 '도로와철도운송' 이
멀쩡히 있는데 `val_eval.sector` 는 None 이었다.

**업종은 회사의 사실이지 밸류에이션 산출물이 아니다.** 모델이 안 돌았다고
업종을 모르는 것은 아닌데 한 덩어리로 묶여 있었다.

### ③ 산출 파일 이름이 안 맞아 96,218건이 묻힘

1차 재백필 산출을 `subscore_sector_*.jsonl` 로 썼다. 그런데 **읽는 쪽
glob 은 여섯 곳 모두 `subscore_patch*.jsonl`** 이다. 8시간 결과가 통째로
안 보였고, 더 나쁘게는 **2차 패스가 1차를 못 알아봐** 처음부터 다시 할
뻔했다(`load_sector_done()` 도 같은 glob).

이름 규칙을 기억에 맡기지 않는다 — `--out` 이 `subscore_patch` 로
시작하지 않으면 실행을 거부한다. 라운드 72 의 백업 별표 누락
(`virtual_predictions*`)과 같은 종류다.

### 결과

| | 시작 | 최종 |
|---|---:|---:|
| 섹터 커버리지 | 47.1% | **89.8%** |
| ⑤ 섹터군집 보정 | 21,987 | **31,574** |
| 국면 커버리지 | 52.4% | **100.0%** |

섹터 빈 곳 18,469건의 **92.6%가 ETF 계열**이다(ETF 282 · 레버리지 13 ·
인버스 5 종목). 업종이 원래 없으므로 `None` 이 맞다 — 지어내지 않는다.
일반 주식은 26종목(1,367건 · 0.75%)만 남았다.

### 파생층 최종

| 층 | 커버 | 빈 곳 사유 |
|---|---:|---|
| 경로 · 진입 기준선 | **100.0%** | — |
| 돌파 플래그 | 89.9% | blind 11,478 · OPEN 6,976 · **설명 안 됨 0** |
| 섹터 | 89.8% | ETF 계열 92.6% |
| 국면 | 100.0% | 56건 — 2014-05 이전이라 지수가 없다 |
| 전방 | 0 | 백필로 만들 수 없다 |

---

## 8. 8/23 동결과의 관계

과거 백필이므로 전방 재평가(R55·R57·R66)에는 영향이 없다 — 그것들은
2026-08-09 이후 새로 쌓이는 데이터로만 판정한다. 점수·게이트·문턱도
바꾸지 않았다. 늘어난 표본으로 **다시 재는** 것은 8/23 이후다.
