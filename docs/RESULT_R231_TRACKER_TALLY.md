# 결과 R231 — 첫 수확을 화면이 그대로 낸다 · 참고 블록은 접는다 (2026-09-07)

## 1. 첫 수확 (전방 추적 · 장 종료 뒤 17:26 실행)

| 기준일 | 확정 | 성공 | 실패 |
|---|---:|---:|---:|
| 2026-08-08 | 5 | 1 | 4 |
| 2026-08-09 | 5 | 1 | 4 |

08-10 5건은 아직 open — 시세 원천에 오늘 봉이 아직 없어 20번째 봉을 못 셌다(내일 아침
실행이 잡는다). 누적 확정 51건: **성공 29 · 실패 15 · 미결 7** (성공 비율 66% · 분모 44 ·
미결 제외 · 전방 표본은 아직 작다 — 이 수로 우위를 말하지 않는다 · §9).

화면의 '실전 추천 추적 파이프라인' 줄은 '동결 N · 확정 대기 N · 마지막 실행'만 적고 있어
이 갈래가 안 보였다. 이제 "확정 N건 — 성공 · 실패 · 미결 (성공 비율 · 분모 · 미결 제외 ·
전방 표본은 아직 작습니다)"를 같은 줄에 낸다. 분모가 0 이면 비율을 만들지 않는다.

## 2. 접은 것 (종목 상세 · 세고 묶고 접기)

'모델 검증 반영 — 이번 판단에 쓰인 근거'(버전·검증 근거 표)와 '다른 원리는 뭐라고 하나 —
참고 (판단에는 반영하지 않습니다)'(채택 안 된 엔진 셋의 참고 표)를 접었다. 제목·내용은
그대로다. `ui_kit.rows_html` 을 새로 두고 `rows()` 가 그것을 그린다 — 그리는 규칙 한 곳.

## 3. 남긴 것 — 두 채점 경로

개장 전 절의 "지난 개장 전 추천의 실제 성과 (사후 검증)"는 `premarket.grade_history`
(prediction_log 채점)로, 추적 줄은 improvement DB(`resolve_long_case`)로 **같은 추천을
따로 채점**한다. 같은 봉에서 목표·손절이 함께 닿은 경우를 한쪽은 '미결'로 두고 다른 쪽은
어느 하나로 셀 수 있다 — 두 수가 어긋날 수 있는 자리(§4). 다음 라운드에서 두 경로를 한
채점기로 모으고, 그 전에 둘의 수를 나란히 재서 적는다.

회귀 §248 · 버전 모델 (ui).
