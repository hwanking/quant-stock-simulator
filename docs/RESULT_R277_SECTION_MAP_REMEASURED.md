# 결과 R277 — 절 지도를 같은 잣대로 다시 쟀다 · 차트 절은 대상이 아니었다 (2026-09-11 · 문서 전용 · 상태표 #12)

## 1. 무엇이 있었나

라운드 229 가 관심종목 절을 5,424 → 4,564px 로 줄이며 *"절 지도(px): 관심종목 4,564 · 차트 5,434 · 점수
요인 4,224 · 판정 3,124. 다음은 차트 절"* 이라 적었다(2026-09-07). 상태표 #12 는 *"차트 절 5,434px — 다음
대상으로 이름까지 적어 두고 안 했다 · 서버·브라우저 측정 필요"* 였다. 고치기 전에 **같은 잣대로 다시
잰다**(R229 의 규칙 — 한 번 고치고 재서 가설이 틀렸으면 그 자리에서 적는다).

## 2. 실측 (8512 · 삼성전자 · 모델 성적 절까지 그린 상태 · 내비 앵커 `nav-*` 사이 거리)

| 절 | 2026-09-07 (R229) | 2026-09-11 (오늘) |
|---|---:|---:|
| 관심종목 (`nav-watchlist`) | 4,564 | **5,836** |
| 점수 요인 (`nav-scores`) | 4,224 | 3,414 |
| 판정 (`nav-verdict`) | 3,124 | 2,945 |
| 가늠 AI (`nav-gaeum`) | — | 1,865 |
| 시장·뉴스 (`nav-context`) | — | 1,593 |
| **차트 (`nav-chart`)** | **5,434** | **1,051** |
| 홈 상단 (`nav-top`) | — | 875 |

차트 절은 오늘 1,051px 다 — 차트 iframe 880px 하나가 거의 전부다. R229 의 5,434 는 오늘 잣대로 재현되지
않는다(그날 어느 상태에서 쟀는지는 문서에 없다 · 지평 탭이 모두 펼쳐진 상태였을 수 있다). **재현되지
않는 수를 근거로 고치지 않는다.** #12 는 '닫음(다시 재니 대상 아님)'.

곁들여 **관심종목 절이 R229 뒤보다 커졌다** — 4,564 → 5,836(32종목 · 보기 모드). R229 가 고친 HTML 표는
그대로이고 그 뒤 R240·R241 이 사유 줄(`why_line`)을 행마다 붙였으니 그쪽이 늘었을 가능성이 있다 —
**추론이고 잰 것이 아니다.** 다음 대상은 이것이며 같은 방법(세고 묶고 접기 · 고치면 다시 잰다)으로 한다.

## 3. 바꾸지 않은 것

코드 · 화면 · 값 전부. 잰 것만 적었다. 상태표: 완료 60 · 부분 3 · 닫음 4 · 남음 15.
