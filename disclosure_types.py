# -*- coding: utf-8 -*-
"""공시 유형 세분화 — **연구 전용.** 운영 분류를 건드리지 않는다.

라운드 139 가 잰 숫자: `market_attention._classify_disclosure` 가 공시의
**58% 를 '기타'** 로 보낸다. 라운드 124 §3.1 이 *"태깅 정확도를 모른 채
이벤트 통계를 내면 그 통계는 태깅 오류의 통계다"* 라고 경고한 그 자리다.

---
## 왜 운영 분류기를 안 고치는가

`_classify_disclosure` 는 관심점수(`market_attention`)로 흘러가고,
관심점수 **상위 5개만 정밀분석**된다. 즉 분류를 바꾸면 **어떤 종목이
스캔되는지가 바뀐다.**

지금 R55·R57·R66 전방 표본이 2026-08-09 부터 쌓이는 중이다. 후보 풀
구성이 도중에 바뀌면 **그 표본이 오염된다.** 그래서 운영 경로는 그대로
두고, 연구용으로만 세분화한다 (2026-11-16 이후 합칠지 정한다).

## 어떻게 §4(경로가 둘이면 안 된다)를 지키나

**운영 분류를 먼저 부르고, 그것이 '기타'라고 한 것만** 세분화한다.
그래서 8종 운영 유형에는 **두 번째 판단이 존재하지 않는다** — 갈라질
여지가 없다.

## 무엇을 세분하나 — 감으로 고르지 않았다

라운드 139 가 모은 실제 공시에서 '기타'로 빠진 제목을 세어, **상위
30개가 기타의 82%** 를 덮는 것을 확인하고 그 덩어리에 이름을 붙였다.
목표는 '기타를 줄이는 것'이 아니라 **각 공시가 무엇인지 아는 것**이다 —
정기·형식 공시를 정확히 이름 붙여야 이벤트 연구에서 **깨끗하게 제외**할
수 있다.
"""
import re

import market_attention as _ma

#: 세분 유형 → 제목에 들어가는 낱말. 앞쪽이 우선한다.
#: ⚠️ 여기 없는 것은 여전히 '기타'다 — 억지로 채우지 않는다 (§3).
EXTENDED_TYPES = (
    # ── 정기·형식 공시: 정보가 아니라 의무 제출물이다.
    #    이벤트 연구에서 **제외하려면** 먼저 이름이 있어야 한다.
    ('하도급·거래조건', ('지급수단별', '분쟁조정기구', '하도급')),
    ('증권발행서류', ('일괄신고서', '일괄신고추가서류', '증권신고서',
                 '투자설명서', '증권발행실적보고서', '증권발행결과',
                 '정정신고')),
    ('주주총회', ('주주총회소집', '주주총회결과', '주주명부폐쇄',
              '의결권대리행사', '기준일설정')),
    ('정기보고서', ('감사보고서', '분기보고서', '반기보고서',
               '사업보고서', '검토의견', '의견부적정')),
    # ⚠️ 라운드 140 — 3일 표본(819건)에서는 안 보이다가 19만 건에서
    #   드러났다. 남은 '기타'의 **38%(12,065건)** 가 이 한 줄이었다.
    #   작은 표본으로 규칙을 정하면 가장 흔한 것을 놓친다.
    ('기업집단·지배구조', ('대규모기업집단현황', '기업지배구조보고서',
                   '지속가능경영보고서')),
    # ── 지분·지배구조
    ('지분변동', ('최대주주등소유주식변동', '주식등의대량보유',
              '임원ㆍ주요주주특정증권', '최대주주변경')),
    ('특수관계·보증', ('타인에대한채무보증', '특수관계인', '계열회사',
                  '동일인등출자')),
    ('경영진변경', ('대표이사변경', '임원변경', '사외이사')),
    ('스톡옵션', ('주식매수선택권',)),
    # ── 사건
    ('소송', ('소송등의', '가처분', '회생절차', '파산')),
    ('손실·위험', ('파생상품거래손실', '손상차손', '횡령ㆍ배임',
               '자본잠식', '거래정지')),
    ('시장조치', ('기타시장안내', '매매거래정지', '상장폐지',
              '관리종목', '투자주의')),
    ('자산유동화', ('자산유동화', '자산양도')),
)


def _norm(t):
    """비교용 정규화 — 공백·괄호·정정 표기를 눌러 낱말만 남긴다."""
    t = re.sub(r'\[[^\]]*\]', ' ', str(t or ''))
    return re.sub(r'\s+', '', t)


def classify(title):
    """공시 제목 → 유형.

    **운영 분류를 먼저 존중한다.** `_classify_disclosure` 가 8종 중
    하나라고 하면 그대로 돌려준다 — 그 8종에는 두 번째 판단이 없다(§4).
    '기타'라고 한 것만 여기서 세분한다.
    """
    base = _ma._classify_disclosure(title)
    if base != '기타':
        return base
    n = _norm(title)
    for label, keys in EXTENDED_TYPES:
        if any(_norm(k) in n for k in keys):
            return label
    return '기타'


def is_routine(kind):
    """이벤트 연구에서 **제외해야 할** 정기·형식 공시인가.

    '기타'는 여기 넣지 않는다 — 모르는 것과 무의미한 것은 다르다 (§3).
    """
    return kind in ('하도급·거래조건', '증권발행서류', '주주총회',
                    '정기보고서', '자산유동화', '기업집단·지배구조')


#: 운영 분류기의 8종 (여기서 다시 정의하지 않는다 — 원본을 읽는다)
def operational_types():
    return tuple(label for label, _keys in _ma.DISCLOSURE_TYPES) + ('기타',)


def all_types():
    return operational_types() + tuple(l for l, _k in EXTENDED_TYPES)
