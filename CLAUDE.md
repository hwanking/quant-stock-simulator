# 가늠 (Gaeum) — 프로젝트 규칙

한국 주식 퀀트 시뮬레이터. Streamlit 단일 앱.
이 파일은 **작업 전에 반드시 읽는다.** 여기 적힌 것은 취향이 아니라
지난 39라운드에서 실제로 사고가 났던 자리들이다.

---

## 1. 절대 건드리지 않는 것 — 로직

UI·레이아웃 작업을 할 때 아래 파일의 **계산부**는 손대지 않는다.

| 파일 | 역할 |
|---|---|
| `quant_indicators.py` | 점수 산식·실행 레벨·게이트 (약 5,300줄) |
| `bitemporal_engine.py` | 시점 복원·누출 차단·데이터 수집 |
| `verdict_core.py` | 중앙 판정 — 모든 화면이 읽는 단일 결과 |
| `price_axes.py` | 적정가 3축 (장기 범위 / 시장 공정가 / 실전 진입가) |
| `regime_policy.py` | 국면별 게이트 |
| `market_context.py` | 뉴스·시장 국면 |
| `sector_cycle.py` | 업황 측정 — 프록시·매크로 (라운드 44, **표시 전용**) |

UI만 바꿀 때는 `web_app.py`의 **렌더 부분**과 `ui_kit.py`만 만진다.
작업 후 `git diff --stat` 으로 계산 파일이 안 바뀌었는지 확인한다.

## 2. 숫자를 손으로 고르지 않는다 (가장 중요)

게이트 문턱·상한·가중치를 **감으로 정하지 않는다.** 이 규칙을 어겨서
세 번 기각했다:

- **라운드 27b** — 블라인드 n=16(적중 12%)으로 신규 매수를 차단했다.
  같은 칸이 학습+검증에서는 n=224에 63.8%였다. 2단계 프로토콜로 다시
  재니 6칸 전부 '정상'이고 개선 효과 +0.0%p. **자기 기각.**
- **라운드 29** — 구간 하한 차이 7.3%p를 상한 60점 차이로 선형 증폭했다.
  기각하고 이미 채택된 구간 규칙(`regime_policy.BANDS`)을 재사용했다.
- **라운드 33** — 진입 깊이 상한을 고정 %(5.5%)로 뽑았더니 변동성 큰
  종목만 잘렸다. σ 단위로 다시 재서 2.1σ로 교체(라운드 35).
- **라운드 44** — "미국 섹터가 호황이면 적정가에 +15~20% 프리미엄"을
  요구받았다. 원장 16,805건으로 실측하니 게이트 미통과 — 특히 절대
  모멘텀은 블라인드에서 **방향이 반대**였다(호황 구간 EV −2.467%).
  기각하고 표시 전용으로 냈다. 겸사겸사 종전 '시장조정' **상수 −2.0%**
  도 걷어냈다 — 근거 없는 −2%보다 근거 없음을 밝힌 0%가 정직하다.

**새 문턱이 필요하면:**
1. 사전등록 — 측정 **전에** 판정 기준을 코드/문서에 적는다
2. 원장(`.portfolio/virtual_graded.jsonl`)으로 실측한다
   — **2026-09-02 기준 250,725건**.
   ⚠️ 종전 이 줄은 *"매 거래일 +400 안팎으로 자란다"* 고 적고 있었는데
   **그것은 사실이 아니었다.** 2026-08-16 부터 **16일간 한 건도 안 늘었고**
   (마지막 케이스 기준일 2026-07-09), 라운드 196 이 그것을 찾아냈다.
   라운드 197~213 이 랩을 **손으로** 여러 번 돌려 184,769 → **250,725**
   (+65,956)로 다시 키웠다 — 라운드 213 에서 8차 배치가 상한(20,000)보다
   적은 18,956 만 더해, **현행 계획 표본을 사실상 소진**했다(원장 250,725
   ≈ 계획 250,726). 더 자라려면 새 거래일이 열려야 한다.
   ⚠️ **여기 적혀 있던 "자동 스케줄이 없다"는 거짓이었다** (라운드 245 ·
   2026-09-08). `.github/workflows/daily_accumulate.yml` 이 평일 08:00 UTC
   (17:00 KST)에 **돌고 있다.** 실행 이력: ~08-18 성공 · **08-19~09-01 연속
   실패** · 09-02~09-07 성공. 종전 문장은 그 실패 구간을 보고 "자동이 없다"로
   읽은 것이다 — **없는 것과 실패한 것은 다르다**(§3 의 그 규칙을 이 파일이
   어기고 있었다).
   ⚠️ 그리고 되살아난 자리가 문제였다. 릴리스 사슬이 `data-20260818` 에서
   끊겨, 09-02 부터 **08-18 스냅샷**에서 다시 시작했다. 손으로 키운 65,566행이
   빠진 원장(185,962)을 자동이 키우고 있었다 — 로컬 250,725 와 갈라졌다
   (공통 185,159 · 로컬만 65,566 · 클라우드만 803). 합집합 **251,528** 로
   합쳤다(원본 `virtual_predictions*` 도 같이 — 랩이 원본에서 원장을 통째로
   다시 쓰므로 산출물만 합치면 다음 실행이 지운다). 지운 행 0.
   ⚠️ **신선도 가드가 그것을 보고도 통과시켰다** — `lag = now - rows` 가
   −64,763 인데 판정이 `lag <= MAX_LAG` 라 음수는 늘 참이었다. 파일 독스트링이
   *"원장은 늘기만 하므로"* 를 전제하고 있었고 그 전제가 깨졌다. 이제 **음의
   밀림은 실패**다. 근거: `docs/RESULT_R245_LEDGER_SPLIT.md`
   *"표본이 곧 늘어난다"* 를 전제로 한 계획(R113 검정력 · R129 표본 상한)은
   자동이 **돌더라도 어느 스냅샷에서 시작했는지**를 같이 본다.
   ⚠️ 그리고 랩은 **줄어들 수도 있다** — 시세를 못 받은 종목의 케이스가
   통째로 빠진 채 원장이 덮어써진다. 라운드 197 이 가드를 넣었지만,
   돌리기 전에 **행수를 찍어 두고 끝나고 대 본다**.
   ⚠️ **그리고 그 250,725건은 독립 사례가 아니다** (라운드 217 · 2026-09-03).
   격자가 **가장 최근 봉**에서 25봉씩 거슬러 뽑히므로 거래일마다 한 봉
   밀리고, 완료 판정은 (종목, 날짜) 정확 일치라 **다른 날 돌릴 때마다 밀린
   격자가 통째로 새 케이스**였다. 같은 종목 이웃 기준일의 72%가 20봉 결과
   창 안에서 겹치고, 채택된 25봉 간격을 실제로 지키는 부분집합은
   **122,554건(48.9%)** 이다(train 106,351 · valid 8,616 · blind 7,587).
   "계획 소진"은 그날 격자 기준이었고, "새 거래일이 열려야 자란다"는
   옛 역사를 한 봉 옆에서 다시 표본화한다는 뜻이었다. 케이스 수를 표본
   크기로 읽은 계산은 전부 실제보다 좁다 — 상관된 표본은 **거짓 양성**을
   늘린다(블라인드 단독 신호가 배치마다 뒤집히던 R213 과 맞는다).
   규칙은 `ledger_view.py` 한 곳(35일 = 25봉×7/5 · R54b 가 이미 쓰던 규칙 ·
   새 숫자 아님). 랩은 같은 종목 35일 안 케이스가 있으면 계획에서 빼고
   (전방 모드 제외), 화면은 겹침 없는 수를 그 자리에서 세어 같이 낸다.
   원장 행은 지우지 않았다. 케이스 수를 인용할 때는 **겹침 없는 수를 같이
   적는다.** 표본 감사(R72 · 같은 종목 35일 재신호 = 한 사건)로 보면 더
   분명하다 — 원장이 184,759 → 250,725 로 **커지는 동안 독립 에피소드는
   57,496 → 46,976 으로 줄었다**(2026-08-16 → 09-03). 밀린 격자가 빈틈을
   메워 사건들을 이어 붙였다. 그리고 그 감사 산출물과 R80 ICC · R54b 업종
   성적(`sector_perf.json`)은 축적 루틴의 갱신 목록에 없어 여덟 배치 동안
   한 번도 갱신되지 않았다(§234 가 이제 셋의 신선도를 동봉본 날짜에 잠근다) —
   헤더는 생성 스크립트조차 없는 R54b 고정본(08-09 · 60,462행)의 32,721 을
   띄우고 있었다. 이제 헤더·캡션은 `sample_audit.json` 을 읽고 기준
   행수·잰 날짜를 같이 내며, §234 가 "기준 행수 = 원장 행수"를 잠근다.
   덤 — R198 의 '원장 행수로 센다' 결정이 네 자리 중 **두 자리**(사이드바·
   홈)에만 닿아 헤더·모델 성적은 `total_cases` 249,748 을 띄우고 있었다.
   고친 헤더가 그 옆에 '원장 250,725건 기준'을 놓자 한 줄에서 드러났다.
   근거: `docs/RESULT_R217_LEDGER_GRID_OVERLAP.md`
   그리고 R217 이 *"다음 일"* 로 남긴 업종 결측 16,954행(17%)을 **라운드
   218 이 갈랐다** — ETF(구조상 없음)는 2,697행뿐이고 **14,102행(83%)이
   보통주인데 같은 종목의 다른 행에는 업종이 있었다.** 생성기가 (종목, 날짜)
   로만 물어서다. 업종은 날짜가 아니라 **종목의 성질**이므로 종목 단위로
   채우고(커버리지 82.8% → **97.1%** — ⚠️ 분모는 **개발 구간 매수권 판정완료
   98,822행**이지 원장 전체 251,528행이 아니다 · 2026-09-10 실측 95,903/98,822 =
   97.0% · 같은 이름의 수가 둘이면 분모를 옆에 적는다 · 라운드 260), 업종이 두
   가지로 기록된 종목은
   **안 채운다**(32행 · 모르는 것을 고르면 그게 지어내는 것이다). 산출물이
   출처별·사유별 셈을 적고 §235 가 **두 항등식**(출처+사유=대상 · Σn=담은
   행수)을 잠근다. **'못 채운 것'과 '없는 것'은 세어 보기 전에는 모른다** —
   R217 은 섞여 있다고 적었고 실제는 83:16 이었다.
   같은 라운드의 덤 둘. ① **회귀가 네트워크 한 번에 죽고 요약도 안 남겼다** —
   3절의 실시세 호출이 시간초과로 터지자 38줄 로그에 트레이스백만 남아 "코드가
   깨졌나 · 네트워크가 흔들렸나 · 몇 건까지 갔나"를 가릴 수 없었다. `atexit` 로
   **중단 요약**을 찍고(마지막 절 + 여기까지의 수 + *"전체 통과가 아니다"*),
   실시세는 한 번 재시도 후 **건너뜀**으로 적는다(통과 아님 · 사유는 예외 원문
   그대로 · R165). 그리고 **내 드라이런 도구가 같은 죄를 지었다** — 절 끝을 다음
   절 머리로 안 끊어 뒤 절을 삼켰고(§233 18건이 51건으로 보였다) 크래시하면 옛
   "실패 0건"이 파일에 남았다. `_probe/_dry_common.py` 한 곳으로 모았다.
   **검사에 요구하는 규율은 검사 도구 자신에게도 적용한다.**
   ② **연구 레이더가 '지금 하는 연구'를 안 담고 있었다** — 화면 단일 출처인
   `data/research_radar.json` 이 08-23 에 멈춰 R215·R216(지금 서 있는 개선
   계획)·R217 이 빠져 있었다. 세 줄을 넣고, §235 가 **가장 최근 사전등록의
   라운드가 레이더에 있는지** 본다(새 사전등록 → 레이더 갱신 톱니).
   근거: `docs/RESULT_R218_SECTOR_COVERAGE.md`
   **같은 모양이 전방 추적에도 있었다** (라운드 222 · 2026-09-04). 실전 추천
   추적 루틴이 3주 서 있어 돌렸더니 이력 218줄이 **전부 새 케이스**로 들어갔다
   — 케이스 열쇠에 모델 버전이 들어 있어 버전이 바뀔 때마다 옛 추천이 통째로
   재동결됐다(463행 중 복사본 245 · 53% · 08-15 엔 같은 85건이 두 번). 그리고
   회귀 §61 의 2099-01-01 픽스처가 실제 이력에 남아 버전마다 유입됐다(7벌 —
   R165 의 "검사가 사용자 자료를 바꾸고 있었다"). 정체는 **(종목, 기준일)**,
   버전은 도장이다. 이미 있는 쌍과 미래 기준일은 건너뛰고 세어 찍는다.
   복사본은 지우지 않고 표시했다(`scripts/mark_tracker_duplicates.py` · 멱등).
   실제 케이스 217 = 고유 쌍 217. §61 은 임시 이력에 쓴다. **완료 판정의
   열쇠에 자주 바뀌는 것이 들어 있으면 옛 것이 매번 새 것이다.**
   덤 — 검사에 SQLite `date('now')` 를 썼다가 틀렸다. UTC 라 한국 아침엔
   어제다. 근거: `docs/RESULT_R222_TRACKER_DUPES.md`
   숫자를 인용할 때는 **잰 날짜를 같이 적는다.** 날짜 없는 숫자는
   반드시 낡는다 (이 줄이 19,883건에서 9배 어긋난 채 보름을 있었다)
3. train / valid / blind 세 구간을 모두 본다. 한 구간에만 기대지 않는다
4. 작은 표본(n<30)은 Wilson 하한으로 본다
5. 기준 미달이면 **기각하고 현행 유지**. 측정 후 기준을 내리지 않는다
6. 가능하면 새 숫자 대신 **이미 채택된 구간 규칙을 재사용**한다
7. **싼 기준부터 잰다 — 비용 순으로 정렬한다** (라운드 170).
   사용자 지적: *"맨날 기각이야, 돈 쓰고 이게 뭐야."* 맞는 말이다.
   R170 은 기준 넷 중 **R4(대상이 10개 이상인가)** 를 마지막에 뒀는데,
   그건 **이미 가진 자료로 몇 초면** 알 수 있었다 — 후보가 19개였으니
   최대 이득도 19개다. 먼저 쟀으면 *"이득 상한이 19개다, 할까?"* 를
   묻고 **20분짜리 적정가 일제 산출을 안 돌려도** 됐다.
   → 사전등록에 기준을 적을 때 **각 기준의 측정 비용을 함께 적고**,
     **싸고 결정적인 것을 앞에 둔다.** 앞에서 미달이면 뒤는 재지 않는다.
   → **이득의 상한**(몇 개가 열리나 · 몇 %p 개선되나)을 재기 전에
     먼저 셈해 본다. 상한이 작으면 그것만으로 접는다 — 기각도 비용이다.

## 3. 없는 값을 지어내지 않는다

- 수집 못 한 데이터는 `None`으로 두고 화면에 "미산출/미수신"과 **이유**를 쓴다
- **못 잰 것으로 거르지 않는다.** 거래대금 미수신 시 유동성 필터를 끄고
  그 사실을 밝힌다 (라운드 37: 이걸 안 해서 2,997종목을 전부 탈락시켰다)
- **데이터 미수신 ≠ 추천 없음.** 두 문장을 반드시 분리한다
- 분모가 0이면 비율을 만들지 않는다 (`0.0% (5/1)` 같은 표시 금지)
- **못 읽은 것을 '다른 값'으로 만들지 않는다** (라운드 164). 숫자만 뽑아
  자리를 채우는 정규화가 문자 포함 KRX 코드 `0040Y0` 을 **`000400`
  (롯데손해보험)** 으로, 이름 `ACE 미국빅테크7` 을 `000007` 로 바꿨다.
  조회 실패 시에는 아예 **삼성전자 지표 뭉치**를 돌려주고 있어
  `0040Y0` 조회가 `price=207,000` 을 받아 왔다 — 라운드 119 가 종목명
  에서 걷어낸 그 결함이 지표 쪽에 남아 있었다.
  **못 읽었다는 표시가 남는 실패**가 조용히 남의 값이 되는 실패보다 낫다.
  판별에 실패하면 `None` 이다 — 자리를 채우지 않는다
- **그 결함이 업종에도 있었다** (라운드 220 · 2026-09-04). 업종 파싱이
  `sise_group_detail…upjong` 링크의 **첫 매치**를 그대로 썼는데, 그 링크는
  페이지에 여러 개다 — 진짜 업종 · '더보기' · **'동일업종 비교' 표의 라벨**.
  회사 정보에 업종 링크가 **없는** 종목에서는 표 라벨이 첫 매치가 되어
  `동일업종 PER` 이 업종으로 기록됐다(원장 108행 · 종목 1개). 화면 업황 칸과
  업종 성적 표에 그대로 나갔다. 고침은 판별을 모듈 함수(`sector_from_html`)로
  빼고 가구를 건너뛰며 **없으면 None**. 25종목 대조에서 달라진 것은 그 1개뿐
  (나머지 24개는 글자까지 같다). 원장 행은 안 지우고 소비자가 **엔진의 판별을
  불러** 빼고 그 수를 사유로 센다. **첫 매치를 그대로 믿지 않는다 — 같은
  모양의 링크가 여러 개일 수 있다.** 근거: `docs/RESULT_R220_SECTOR_LABEL.md`
- **지어낸 재무지표가 아직 한 곳에 살아 있었다** (라운드 167). `:787` 주석이
  *"구버전은 eps=현재가×0.08, bps=현재가×0.7 … 통째로 지어내 DB에 넣었다"*
  고 적어 둔 그 결함의 **생존자**가 `generate_synthetic_bitemporal_data`
  첫 줄에 있었다:

  ```python
  meta = STOCK_METRICS_DB.get(symbol, {"eps": 5000.0, "bps": 45000.0,
                                       "pbr": 1.5, "roe": 12.0, ...})
  ```

  `.get(symbol, 기본값)` 이라 **처음 보는 종목**은 통째로 저 리터럴을
  받았다. DB 를 채우는 호출이 **바로 다음 줄**이었으니 순서만 뒤바뀐
  것이다 — **읽고 나서 채우고 있었다.**
  실측(차가운 프로세스 · 실제 파이프라인): 재무가 공시되지 않는 종목이
  전부 **같은 가짜 적정가 52,714원 · 신뢰도 70.8 · CALIBRATED** 를 받았다
  (롯데리츠 참값 3,915 · SK리츠 5,672 — 상태도 CAUTION 이어야 한다).
  **화면에 그대로 나갔다.**
  → 고침은 한 줄이다: **먼저 채우고 나서 읽는다.** 없으면 `{}`.
  → 실측 영향 범위: **1,499종목 중 26종목(1.7%)** — 거의 전부 리츠·
    인프라펀드다(구조상 EPS·BPS 를 게시하지 않는다). 대부분은 무사했지만
    그 26종목은 **적정가가 통째로 지어낸 값**이었다.
  → 이걸 여태 못 본 이유가 더 중요하다. 한 프로세스에서 종목을 이어
    돌리면 **두 번째부터는 DB 가 데워져 정상값**이 나온다. 회귀도 앱도
    늘 데워진 상태였다. **차가운 프로세스로 한 종목만** 돌려야 보인다
  → 그리고 이걸 잡은 것은 **이상값 하나**였다. ETF 룩스루를 재다가
    리츠 ETF 가 NAV 의 **13.4배**로 나왔고, 파 보니 구성종목 여섯이
    **정확히 같은 적정가**를 받고 있었다. **같은 값이 여러 종목에
    반복되면 그것이 신호다** — 분포를 찍으면 보인다.
    근거: `docs/RESULT_R167_ETF_LOOKTHROUGH.md`

## 4. 화면 값은 한 곳에서 나온다

모든 화면(추천 카드·종목 상세·가늠 AI·차트·보유자)은 `verdict_core.build()`
결과 **하나만** 읽는다. 경로가 둘이면 한쪽만 고치는 일이 생긴다.

- 신규 매수자 값(`new_stop`/`new_target`)과 보유자 값(`hold_stop`/`hold_trim`)은
  **다른 키**로 분리한다. 섞이면 버그다
- 정합(손절 < 진입 < 목표)이 깨지면 값을 **고치지 말고 비운다**
- `test_pipeline_fixes.py` §106이 같은 스냅샷을 두 경로로 흘려 16개 값을
  대조한다. 다르면 실패한다
- **쓰기만 하고 아무도 안 읽는 세션 키는 죽은 버튼이다** (라운드 164).
  관심종목 이름 버튼 두 곳이 `st.session_state['ticker_input']` 에 코드를
  넣고 rerun 했는데 **그 키를 읽는 곳이 저장소 어디에도 없었다.**
  캡션은 "이름을 누르면 그 종목을 봅니다"라고 적고 있었고 회귀는 내내
  초록불이었다. 같은 파일에서 `_last_analysis_sec` 도 그랬다
  ("예상 시간을 알려주려고 기억해 둔다"고 적고 그 다음이 없었다).
  §201 ⓜ이 AST 로 **쓰기 횟수 = 전체 등장 횟수**인 키를 잡는다 —
  종목으로 넘어가는 길은 `_go_stock` 하나다
- **신호와 판정을 두 경로로 세지 않는다** (라운드 214 · 세 번째 사례).
  사용자: *"확장 신호 1종목 — SK이노베이션(59점)인데 실제로 손댈 수 있는
  후보는 DB손해보험만. 정리해줘야 하는 거 아냐?"* 배너는 **원점수 띠**
  (58~59 · 60+)로, 실행 후보는 **`verdict_core` bucket** 으로 갈랐다 —
  R114 가 고친 그 모양이 목록에 남아 있었다. 둘 다 사실이지만 **왜
  신호가 판정에서 빠졌는지**를 배너가 말하지 않아 모순으로 읽혔다.
  고침: 판정은 코드마다 한 번(`_pick_of` 메모), 가르는 함수 하나
  (`_sig_class`), 배너 종목 옆에 bucket, 요약 한 줄로 잇는다. §231.
  → 같은 종목이 **두 이름표**(신호/판정)를 달 때는 그 이유가 같은 줄에
    있어야 한다. 없으면 사용자는 엔진이 틀린 줄 안다
  → **'불가' 한 낱말이 셋을 뭉뚱그렸다** (라운드 221 · 2026-09-04). 사용자:
    *"진짜 다 물타기 불가이던데 진짜 맞아?"* 보유 16행 **전부**가 6조건 중
    같은 하나('신규 진입 조건 통과')에 걸렸고 나머지 다섯은 행마다 달랐다
    (10·4·4·1건). 원장 250,725건 중 신규 매수 제목은 **4건** — 그 게이트는
    사실상 늘 닫혀 있어 그것을 요구하는 물타기도 늘 불가다. **계산은 맞다.**
    그러나 ① 시장 게이트 하나에만 막힌 것(포지션 조건 5개 통과 · 6행)
    ② 포지션 조건 미달(6행) ③ 표본·데이터 게이트를 못 넘어 **판단하지 않은**
    것(4행)이 같은 낱말이었다. ③은 불가가 아니라 미판정이다(§3). 고침은
    `ui_kit.avg_down_class` 하나 — 찍힌 실패 목록의 **이름**만 읽어 넷으로
    가른다. 문턱 없음. 조건 이름은 엔진 리터럴과 같아야 하므로 §238 이
    잠근다. **전 종목이 같은 답이면 먼저 조건별로 센다** — R37 의 그 모양.
    근거: `docs/RESULT_R221_AVGDOWN_CLASS.md` · 일정: `docs/SCHEDULE_R221_FORMULA.md`
  → **숫자만 보여 주면 그게 판단이 된다** (라운드 223 · 2026-09-04). 사용자:
    *"섹터별로 좋은지 평가를 내리는 게 좋지 않을까?"* 포트폴리오 화면은 업종별
    비중·원장 적중(53~70%)·HHI·국면을 이미 세어 보여 주고 있었다. 없던 것은
    **그 숫자를 어떻게 읽어야 하는지** — 업종별 적중 차이는 표본·국면 조성 차이
    안에 있어 업종을 고르는 근거가 못 된다(R46 기각 · R181 재측정은 11-16 뒤).
    그 말이 없으면 사용자는 높은 업종을 고르라는 뜻으로 읽는다. 한 줄을 넣었고
    날짜는 `forward_eval` 에서 읽는다. 덤 — 면책 문구가 '원장 184,759건'을
    날짜 없이 박고 있었고(지금 250,725), ETF 에 "종목을 열면 채워집니다"라고
    적고 있었다(구조상 없다 · R218). 판단 엔진은 R181 이 통과해야 연다 — 그
    전에 만들면 §2 다. 일곱 축의 상태·일정: `docs/SCHEDULE_R223_AXES.md`
  → 같은 라운드의 덤: **세션엔 있고 파일엔 없었다.** 관심종목 파일은
    `portfolio.WATCH_SNAP_*` 화이트리스트 키만 저장한다 — 새 `snap_*` 키를
    화면에 찍어도 여기 안 넣으면 재시작에 사라진다(R169 도 같은 자리).
    숫자 칸은 0 을 버리므로 bool 은 글자('가능'/'불가')로, 세션도 같은
    모양으로. 검사는 **저장→읽기 왕복**뿐이다 — 문자열 검사는 못 잡는다

  → **늘 닫힌 게이트가 다른 이름으로 재사용되고 있었다** (라운드 224 · 2026-09-04).
    사용자: *"물타기도 스마트하게 · 정리하는 이유도 써줘 — 적정가는 있는데 너무
    오래 기다려야 한다 · 1차 매도가가 넘었다 이런 것도."* 물타기 첫 조건 "신규 진입
    조건 통과"는 코드에서 `eligible_for_top3`(TOP3 선정용 15게이트 전부 통과 · 표본외
    검증 게이트가 설계상 닫힘 · 원장 매수 의도 제목 56/250,725)를 읽어 보유 16행이
    전부 불가였고, "더 살 수 있나"의 답이 셋(TOP3 깃발 · bucket · actionable)이었다.
    사전등록 R224 로 "하락 중이라는 사실이 신규 매수 판정이 못 보는 정보를 담나"를
    먼저 쟀다 — 25봉 부분집합(R217 재현 일치) 매수권 56,911쌍 · 세 구간 CI95 모두
    0 포함(train +1.7 · valid +3.9 · blind −5.8%p · 블라인드 날짜 32) → (n). 첫 조건 =
    `verdict_core.actionable`(`new_entry_ok` · 호출부 셋이 `_core_of_snapshot` 하나로
    넘긴다 · 안 넘기면 보류). 그리고 **보유자 기준값이 닿을 수 없었다** — 현재가
    ±k 로 다시 계산되는 값을 방문마다 덮어써 목표는 따라 오르고 손절은 따라
    내렸다. `portfolio.hold_plan_update` 가 잰 날에 고정하고(원장 채점과 같은 모양 ·
    20봉=28일 · `ledger_view.bars_to_days`) 닿거나 끝났을 때만 다시 재며 사유를
    `snap_hold_log` 에 남긴다. 정리 사유(`hold_why`)는 잰 것만 — 적정가 거리는
    "같은 국면·구역 원장 n건 중 20봉 안에 그만큼 오른 비율 p%" 로 낸다(시간은 못
    잰다 · '낮다'의 기준을 정하면 §2). 종목 상세의 옛 '물타기 안전성 계산기'
    (1.15/1.05/0.95/0.85 · 68/50 · '30% 축소' · 평단 낮추기 주수 · 현재가×1.05)를
    걷어냈다. 근거: `docs/RESULT_R224_AVG_DOWN.md` · §241
    덤 둘 (2026-09-07 · 보유 16행을 앱의 채우기 버튼으로 다시 채우며). ① **이득 상한은
    어느 시점의 값 위에서 선 것인지 같이 적는다** — R1 은 옛 스냅샷의 bucket 으로 "상한
    3"을 셌는데 다시 채우니 bucket 도 오늘 값으로 바뀌어 **4** 가 열렸다(가능 4 ·
    시장게이트 3 · 포지션미달 5 · 보류 4). ② **빈 글자도 값이다** — 실패가 없으면 ''
    를 찍었는데 스냅샷 병합은 None·'' 를 "못 낸 값"으로 보고 건너뛰어, '가능'으로 바뀐
    4행이 옛 실패 목록을 안고 있었다(R223 의 "숫자 칸은 0 을 버린다"와 같은 모양).
    '없음'(`ui_kit.AVG_DOWN_NO_FAIL`)으로 찍고 읽는 쪽이 빈 목록으로 본다.
  → **한 화면에 오늘 값과 계획 값이 같이 보이면 어느 것이 기준인지 말한다** (라운드 225 ·
    2026-09-07). 종목 상세 "이미 갖고 있다면" 카드만 four_scores 를 직접 읽고 있었고
    (§4 · CORE 로 바꿈), R224 의 고정된 보유 계획과 오늘 다시 잰 값이 한 화면에 같이
    보이게 되자 카드가 어느 것이 관리 기준인지 말하지 않았다 — 계획이 있으면 한 줄로
    적는다(값은 관심종목 행에서 그대로). 보유자 행동점수(옛 가중·문턱)는 그 자리에서
    "실측되지 않은 옛 값"이라 말한다. 값·판정 불변. `docs/RESULT_R225_HOLDER_CARD.md` · §242
  → **긴 것은 세고 묶고 접는다 — 판단은 안 바꾼다** (라운드 226 · 2026-09-07). 사용자:
    *"포트폴리오 너무 길다 · 핵심만 · 잘 짜여 있는지 · 어디를 채울지."* 견해 절이 14,882자
    였고 보유 행마다 같은 문장이 되풀이됐다. 위에서 아래로 규모 타일 · 지금 할 일 칩(표의
    kind 를 급한 순으로 · 손댈 것만 이름) · 구성 막대(상위 6 + 기타 · HHI) · 채울 것(값이
    비어 판단이 비는 자리 · 이유와 채우는 길) · 국면 한 줄 · 상세는 접고(같은 함수의 짧은
    판 `hold_brief`) · 면책 한 줄은 접지 않는다 → 1,102자. "잘 짜여 있는가"에 점수를 내지
    않는다(원장이 종목 단위 · R214 · §2). 부품은 `ui_kit.chip_row`·`bar_list`. 덤 — 검사
    슬라이스의 끝 앵커('전체 비우기')가 사이드바에도 있어 `index()` 가 앞의 것을 잡았고,
    빈 슬라이스를 이어 붙여 파일을 한 번 망가뜨렸다. **끝 앵커는 시작 뒤에서 찾는다.**
    `docs/RESULT_R226_PORTFOLIO_GLANCE.md` · §243
  → **사용자는 라운드 번호를 모른다** (라운드 227 · 2026-09-07). 화면 문자열 41건에 §3 ·
    R224 · (R46) · 라운드 52 같은 내부 참조가 섞여 있었고 물타기 판정 문장은 반말이었다.
    근거의 **날짜·표본 수**는 남기고 번호는 문서로, 판정 문장은 합니다체로. §244 가 두 화면
    모듈의 문자열을 AST 로 훑어 0건을 잠근다(CSS 주석 제외 · 심기 양방향). 옛 낱말을 잠갔던
    검사 셋은 번호 대신 **문장**을 잠그도록 옮겼다. `docs/RESULT_R227_COPY_REFS.md`
  → **인터페이스의 첫 결함은 배치가 아니라 시간이었다** (라운드 228 · 2026-09-07). 첫 진입
    마다 시장 스캔(순위 수집 + 후보 정밀 분석 · 2~3분)이 홈보다 먼저 돌아 홈 첫 그림이
    150~200초였다. 오늘 결론은 개장 전 고정 파일에 있고 장중 재계산 금지가 규칙인데,
    매 세션의 재스캔은 규칙상 화면에 못 쓰는 값을 만들며 시간을 쓰고 있었다. 그리고
    리포트 열쇠가 날짜×엔진 버전이라 화면·문구 배포 셋 뒤에 **똑같은 파일 넷**이 생겼다
    (R222 의 그 모양 — 정체는 날짜, 버전은 도장). 고정 파일이 있으면 스캔을 미루고
    홈을 먼저 그린다('최신화'가 스캔) · 같은 날은 다시 만들지 않고 드리프트를 도장으로
    말한다. 홈 10초 · 관심종목까지 50초 · 전체 80초. 값·판정 불변.
    `docs/RESULT_R228_HOME_FIRST.md` · §245. 덤 — Bash 헤어독은 백슬래시를 반으로
    접는다(`\n` → 줄바꿈). 이스케이프가 든 치환은 파일 스크립트(Write)로 한다.
  → **높이의 주범은 입력이 아니라 위젯 행이었다** (라운드 229 · 2026-09-07). 관심종목 절이
    5,424px 로 가장 길어 입력·빼기를 편집 토글 뒤로 숨겼더니 **5,464px — 안 줄었다.**
    `st.columns` 행 자체가 ~150px 다. 보기 모드를 HTML 표 하나(행 ≈ 58px · 이름은 `?pick=`
    링크 · 버튼과 같은 경로)로 그리자 표는 3.5분의 1, 절은 4,564px. **한 번 고치고 재서
    가설이 틀렸으면 그 자리에서 적는다** — 첫 가설이 틀린 채로 "줄였다"고 적을 뻔했다.
    신뢰도·손익 포맷터는 한 곳(`_wl_fair_conf` · `_wl_pnl`)으로 빼 두 모드가 같이 부른다.
    `docs/RESULT_R229_WATCHLIST_VIEW_MODE.md` · §246
  → **"다 같은 답"이면 조건별로 세고, 그 시점에 그 답이 얼마나 흔한지도 잰다** (라운드 230 ·
    2026-09-07). 사용자: *"다 보유 유지인데 맞아?"* 보유 16행의 현재가는 버틸 수 없는 가격
    까지 내려갈 폭이 3.0~9.3%(중앙 5.7%), 1차 매도가까지 오를 폭이 2.1~6.5%(중앙 4.0%) — 어느 선에도 안
    닿았으니 맞다. 원장에선 두 선 중 하나에 1봉째까지 21% · 3봉째 54% · 20봉째 96%가 닿는다
    — 계획 첫 날엔 흔한 그림이고 사흘 안에 절반은 표가 바뀐다. 이 답을 견해가 먼저 낸다
    (`ledger_view.touch_cdf` · 표시 전용 · 문턱 없음). `docs/RESULT_R230_HOLD_CHECK.md` · §247
  → **나쁜 수는 나오는 날 화면에 그대로 낸다** (라운드 231 · 2026-09-07). 전방 추적의 첫
    수확이 08-08·08-09 열 건 중 성공 2 · 실패 8 이었는데 화면 추적 줄은 '확정 대기 N'만
    적고 있었다. 확정의 갈래(성공·실패·미결)와 분모를 같은 줄에 낸다 — 분모 0 이면 비율
    없음. 남은 것: 같은 추천을 `premarket.grade_history` 와 improvement DB 가 따로 채점한다
    (§4 · 다음 라운드). `docs/RESULT_R231_TRACKER_TALLY.md` · §248
  → **한 페이지에 두 채점기가 있었고, 한쪽은 체결된 적 없는 가격으로 수익을 냈다** (라운드
    232 · 2026-09-07). 개장 전 절의 '사후 검증'은 `prediction_log`(진입 = 리포트 가격 · 닿으면
    즉시 · 이력 마지막 100행)로, 추적 줄은 improvement DB(진입 = **권장매수가** · 20봉 뒤 ·
    같은 봉은 미결 · MDD 는 청산 뒤까지)로 같은 추천을 셌다 — 실측 95건(목표 30 · 손절 26)
    vs 확정 51건(성공 29 · 실패 15). 등급은 51/51 같았지만 39건이 권장매수가 진입이고 **18건은
    그 가격에 닿은 적이 없다** — 중앙 수익 +10.4% vs +6.4%(§3 의 지어낸 체결 · §9 의 부풀림).
    원장 250,725행이 쓰는 규칙이 앞쪽이므로 그쪽으로 모았다: 선도달 규칙은
    `prediction_log.first_touch` **한 곳**, 루틴은 원장과 같은 `grade_prediction`(닿으면 그
    자리에서 확정), 화면 두 자리는 `case_tracker.tally` 한 곳을 **읽기만**, 확정 51건은 재환산
    (판정 바뀜 0). *"두 계층은 섞지 않는다"* 는 독스트링은 화면이 둘을 한 페이지에 낸 순간
    깨져 있었다 — **견주려면 규칙이 같아야 한다.** `docs/RESULT_R232_ONE_GRADER.md` · §249
  → **한 조건에 알림 셋 · '0건'과 '12건'이 한 화면 — 지평별로 세고 한 번만 말한다** (라운드 233 ·
    2026-09-07). 점수 요인 절(3,672px)의 기본 탭에서 20일 표본 부족 한 조건에 알림이 셋이었고,
    엔진의 `match_count`(= **20일 지평**)를 '유사패턴 표본 0건'이라 세 번 말한 뒤 아래 그래프는
    60일 표본 12건을 그렸다(40일은 0건이라 기본 지평이 60일로 밀렸다). 둘 다 맞는데 어느 지평인지를 안 말해 모순으로 읽혔다(R214 의 두
    이름표). `ui_kit.horizon_counts_line` 로 지평별 수를 한 줄로 먼저 세고 게이트는 알림 하나 ·
    7단계 설명은 접고 · 빈 20일 타일 여섯은 문장 하나 · 같은 값 타일은 한 번. 3,672 → 2,976px.
    값·판정 불변. **같은 이름의 수가 둘이면 단위(지평·기간)를 이름 옆에 적는다.**
    `docs/RESULT_R233_PREDICT_TAB_ONCE.md` · §250
  → **라벨과 데이터의 지평이 같아야 한다 · 기준은 규칙집 한 곳에서 읽는다** (라운드 234 ·
    2026-09-07). 사용자: *"라벨은 기본 40일인데 데이터는 60일 · 엔진이 고른 10일을 무시 · 기준이
    5건인지 10건인지 · 20일 0건인데 5·10·60일 값이 뒤섞였다."* 라벨 "기본 40일"이 글자로 박혀
    있었고 40일이 0건이면 60일로 밀렸다. 5/10 은 모순이 아니라 다른 기준(`SAMPLE_TIERS`: 5 =
    관찰값 하한 · 10 = 확률 하한)인데 화면이 둘을 따로 말했다. 순서를 결론 한 줄 → 기간별 표(방향
    포함) → 20일 확률(없으면 카드 셋 대신 한 줄) → 다른 기간(기본 = 엔진의 관찰 점수 최고 기간 ·
    제목에 지평·n·등급)로 바꾸고, '방향 일치 67점'은 분자·분모(상승 2 · 하락 1)와, '최적 관찰
    기간'·'전략 유형 확률'은 추천·확률로 안 읽히는 이름으로. rho 커버리지 감사는 레이더 후보
    (운영 rho 0.80 불변). `docs/RESULT_R234_PREDICT_TAB_COHERENCE.md` · §251
  → **이름이 가리키는 것과 그 자리에 넣은 값이 다르면, 그건 표시 오류가 아니라 지어낸 값이다**
    (라운드 235 · 2026-09-08). 사용자: *"같은 60일선이 한 행에서는 267,537원, 다른 행에서는
    263,843원이다."* 시나리오 표가 **현재가 × (1 ± 변동성 배수)** 를 만들어 '20일선'·'60일선'
    이라 부르고 있었다 — `tech_df` 에 `sma_20`·`sma_60` 이 **이미 있는데 한 번도 안 읽었다.**
    같은 이름이 두 값이 된 것은 두 행이 다른 배수를 썼기 때문이다(R164·R220 과 같은 모양 · §3).
    곁들여: `atr_val` 은 ATR 이 아니라 20일 일간수익률 표준편차 · 변동성 미수신이면 `0.02` 로
    가격을 만들었다 · 횡보 '1차 목표'가 지지선보다 낮았다 · 하락 행 손절이 중앙 판정 값이
    아니었다(§4). 고침은 실제 이동평균을 읽고(없으면 '확인 불가'), 변동성 기준선은 그렇게 부르고,
    정합성이 깨지면 숫자를 맞추지 말고 미산출로 두고, 청산 기준은 중앙 판정에서 가져온다.
    **배수는 하나도 안 바꿨다**(§2). 그리고 **화면을 보다가 내가 넣은 문구의 뜻이 뒤집힌 것을
    잡았다** — 하락 확인선을 '무효화선'이라 적었다. 회귀는 통과했다(글자는 맞고 뜻만 틀렸다 ·
    §6 의 "화면 결함은 서버를 띄워야 나온다"). `docs/RESULT_R235_SCENARIO_MA_LABELS.md` · §252
  → **상태값에 주어가 없으면 사용자는 옆의 수를 주어로 읽는다** (라운드 236 · 2026-09-08).
    사용자: *"Buy Setup 0/9 인데 Perfected 가 충족이다 — 렌더링이 꼬인 것 아닌가."* 아니다 —
    엔진의 `perfected_status` 는 **가장 최근에 완성된 9 셋업**을 두고 하는 말이라 진행 중인
    카운트와 다른 시점의 사실이다. 계산은 맞고 **주어가 빠져 있었다.** 그래서 사용자가 제안한
    *"9 미만이면 미해당으로 덮어써라"* 는 안 했다 — 있었던 사실을 지우는 일이다. 주어를 적었다.
    곁들여 '미충족'이 둘을 한 낱말로 말하고 있었다(완성된 셋업이 없었다 · 있었지만 못 채웠다) —
    가르지 않는다고 적는다(§3 · R221 의 '불가' 와 같은 모양). 그리고 탭 머리의 46점이 아래
    'Bullish 6 · Bearish 11' 과 안 이어져 있었다: `50 + (6 − 11) × 0.9 = 45.5 → 46`. 산식 문장은
    **엔진이** 내고 화면은 읽기만 한다 — 화면이 산식을 다시 적으면 계수가 바뀔 때 문장만 낡는다
    (§4). §253 이 산식 줄과 문장 줄을 같이 잠근다. `docs/RESULT_R236_DEMARK_SUBJECT.md` · §253
  → **필수 전제를 다른 조건과 같은 무게로 세면 이름이 가리키지 않는 상태를 그 이름으로 부른다**
    (라운드 237 · 2026-09-08). `check_20sma_settlement` 이 네 조건 중 **3개**면 '안착 성공'이라,
    가격이 20일선 **아래**여도(가격 조건 실패) 기울기·거래량·RSI 셋이 맞으면 성공이 나왔다
    (사용자가 합성 입력으로 재현 · §254 가 심는다). '안착'은 그 선 위에서 유지된다는 뜻이므로
    가격 조건을 **필수 전제**로 뺐다 — 문턱(2봉·0·1.2배·45)과 '3개' 규칙은 불변이고, 호출부가
    화면 한 곳뿐이라(반환 불리언은 아무도 안 읽었다) 운영 판정 영향은 없다. 곁들여 **전처리의
    결측 대체값(기울기 0.0 · RSI 50.0)이 '충족'으로 세어지고 있었다** — 기울기는 결측이면 항상
    0 이상이라 통과했다(못 잰 값이 가드를 통과시킨 자리). 지표가 이미 쓰는 창을 못 채우면
    '확인 불가'로 적고 세지 않는다(§3). 그리고 관점 이름 '수급·기술'은 계산(볼린저 위치·RSI
    둘의 평균)보다 넓어 33점이 "수급까지 본 종합"으로 읽혔다 — 이름을 '가격 위치 (볼린저·RSI)'
    로 좁혔다. **이름이 계산보다 넓으면 사용자는 없는 근거를 있다고 읽는다.** 사용자가 요구한
    '수급 총알 추가'·'외국인 순매도가 원인' 브리핑은 **안 했다** — 그 점수에 수급이 없으므로
    지어낸 설명이 된다(§3). `docs/RESULT_R237_SETTLEMENT_PREMISE.md` · §254
  → **지울 수 없으면 드러낸다 — 근거 없는 상수를 화면이 숨기지 않는다** (라운드 238 ·
    2026-09-08). 사용자가 화면의 두 수만 보고 짚었다: *"기초 275,844 → 적정가 270,327, 그 −2.0%
    는 어디서 왔나."* `raw_target_val = weighted_median * 0.98` 이 **최초 커밋부터** 있고 근거가
    코드에도 문서에도 없다 — **R44 가 걷어낸 것은 별도의 `market_adjustment_pct: -2.0` 이었고
    이것은 그 정리의 생존자다**(R167 과 같은 모양). 게다가 화면의 '기초 펀더멘털 가치'는 최종값을
    0.98 로 **되나눈** 값이라 원인을 결과에서 역산한 라벨이었다. 지우면 **모든 종목 적정가가 2%
    움직여** 구역·게이트·권장 매수가가 바뀌므로 §2 순서(사전등록 → 영향 측정 → 결정)를 지킨다 —
    그때까지 **사슬을 화면에 그대로 낸다**(가중중앙값 → 고정 보정 → 수축 → 업황 → 최종 · 근거가
    없다는 사실까지). 실행 확인(삼성전자): 275,844 × 0.98 = 270,327. 곁들여 배너('적정가 이하')와
    상세('+0.1% · 적정가 부근')가 한 화면에서 다른 말을 했다 — **구역 낱말은 게이트가 읽으므로
    못 바꾸고**, 배너 문장에 괴리율과 이미 채택된 띠 낱말을 같이 넣었다(제안된 ±3% 하드코딩은
    새 문턱이라 쓰지 않았다 · §2). 업황 사유는 같은 탭에 세 번 → 한 번.
    `docs/RESULT_R238_FAIR_VALUE_CHAIN.md` · §255
  → **이름이 약속하는 자료를 실제로 받고 있는지 본다** (라운드 239 · 2026-09-08). 화면이
    'FCFF (DCF)' · 'EV/EBITDA' · 'SOTP / 조정 NAV' · 'EV/Sales & GP' 를 띄우고 같은 표에
    'WACC 8.5% / 영구성장률 2.0%' 와 '미수신 입력 지표: 없음' 을 나란히 적었다 — 세 줄이 함께
    읽히면 "현금흐름·EBITDA·매출을 각각 받아 교차검증했고 빠진 자료도 없다"가 된다. **셋 다
    아니다**: 수신 재무는 EPS·BPS·PBR·PER·ROE·부채비율 **여섯뿐**이고(매출·순이익·영업현금흐름은
    None, EBITDA·배당금·사업부는 필드 자체가 없다) 여덟 모형이 전부 그 여섯에서 파생된다
    (`ebitda_ps` 조차 EPS·BPS 로 만든 추정치 · FCFF 는 실질 `정상화EPS × 8.76배` · SOTP 는
    `BPS × 1.275`). **이름만** 실제 입력에 맞췄다 — 산식·계수·가중치·계산 키 불변, 적정가
    270,327원·신뢰도 76.9 그대로. 이름 문자열은 정의 줄 한 곳에만 있고 게이트·정렬에 안 쓰인다.
    **정교한 이름이 붙은 간편 추정값은 그 이름만으로 근거가 된다** — R220 의 '첫 매치를 그대로
    믿지 않는다' 와 같은 계열. `docs/RESULT_R239_MODEL_NAMES.md` · §256
  → **감사에 워크플로를 쓸 때도 판정은 적대적 검증을 거친다** (라운드 239). 밸류에이션 큐 6건을
    병렬 감사(감사 6 + 반박 6)했더니 6건 전부 실재였으나, 그중 **하나는 제안이 실행 불가**였다
    (넣으려던 캡션 자리가 하나의 HTML 블록 안이라 표 아래로 밀린다) **하나는 사유가 거짓**이었다
    ('표본외 검증 미수행'은 실제 사유가 아니라 계산 순서 탓이다 · §3). 검증자가 없었으면 둘 다
    나갔다. 감사자의 배수 계산 셋도 반박 단계에서 정정됐다(FCFF 13.6 → 8.76 · DDM 8배 어긋남).
  → **결론만 적고 사유는 안 적으면, 맞는 답도 모순으로 읽힌다** (라운드 240~241 · 2026-09-08).
    사용자: *"미보유 추천 제외는 왜인지 간략하게 써주면 좋을 듯. 적정가는 현재가보다 높은데 추천
    제외라고 하니깐."* 사유는 중앙 판정이 결론과 **함께** 내고 있었는데(`exclude_reason`) 스냅샷이
    결론만 담아 화면까지 오지 못했다. 적정가는 재료 하나이고 비용 차감 기대값·손익비·유동성·과열·
    국면이 막으면 그쪽이 이긴다. 옮겨 적었다. 그리고 **'빼기'는 사라진 적이 없다** — 편집 토글
    안에 있는데 이름이 '매입가·수량 편집'이라 알 수 없었다(→ '· 빼기' 추가 · 기능은 안 옮겼다).
    R241 이 그 R240 을 다시 열었다: 사유가 **없을 때 빈 글자**를 찍어 ① 파일에 키가 안 생기고
    (31행 중 사유가 보인 행 **1**) ② 병합이 건너뛰어 **제외가 풀린 행에 옛 사유가 남았다**(초록
    '매수 가능' 아래 '추격매수 위험'). R224 덤 ②('빈 글자도 값이다')와 같은 모양이라 고침도 같다 —
    마커(`ui_kit.WATCH_NO_WHY`)로 남기고, **보여 줄지는 킷 한 곳**(`watch_action.why_line`)에서
    정하고, 사유 없는 미보유 행(14행)을 채우기 대상에 넣되 배너가 *"값은 다 있고 판정 사유만
    없다"* 를 따로 적는다. `docs/RESULT_R240_WHY_AND_REMOVE.md` · `docs/RESULT_R241_WHY_PERSIST.md` · §257·§258
  → **검사의 대상과 판별식은 같이 낡는다 — 네 번째다** (라운드 242 · 2026-09-08). R227 이 화면
    문구에서 내부 참조를 걷어낸 그 검사가 대상을 `('web_app.py','ui_kit.py')` 로 **손으로** 적고
    있었다. 유도하니 **38개 모듈 · 화면 문자열 2,596개**에 참조 **25건**. 게다가 판별이 **자기가
    보던 파일 안에서도** 하나를 놓쳤다 — `f"…{hi}%] · R59"` 의 끝조각에는 한글이 없어 '한글 있음'
    조건을 빠져나갔다. **f-string 은 조각이 아니라 덩어리로 본다.** 대상은 유도로, 판별은
    `scripts/lineage_audit` 한 곳으로. 24건을 고쳤고 **박제된 `forward_eval.py` 는 안 고쳤다** —
    2026-11-16 전방 재평가 대상이라 화면 문구라도 바꾸면 평가가 성립하지 않는다(한 번 고쳤다가
    박제 대조에 걸려 되돌렸다 · **문구 규칙보다 측정 약속이 앞선다**). 검사는 박제 파일을 건너뛰되
    **몇 개를 왜** 건너뛰는지 찍는다. 덤 ① 진입가 근거가 *"실측 체결률 79.3% · 평균 3.4일"* 을
    **날짜 없이** 적어 이 종목 값으로 읽혔다 — 2026-08-04 · 경로 5,389건이라고 적었다(수치 불변).
    덤 ② 회귀 1건이 장중에만 실패했는데 **검사가 반올림된 `atr_pct` 에서 배율을 다시 계산**한
    탓이었다 — 엔진이 **쓴 배율을 출력에 싣고**(`next_action.band_scale`) 규칙 자체는 심어서 잰다.
    그리고 이 조사를 하던 **내 임시 도구가 0개 파일을 보고 "0건"** 을 찍었다(경로를 잘못 만들었고
    본 파일 수를 안 찍었다) — **검사에 요구하는 규율은 조사 도구에도 적용한다.**
    `docs/RESULT_R242_SCREEN_REF_SCOPE.md` · §244(확장)·§259
  → **같은 수가 두 이름으로 불리면 무엇을 가르는 이름인지 적는다** (라운드 243 · 2026-09-08).
    같은 신뢰도 77점이 배너에서는 '정상 표시'(중심 적정가를 **숫자로 낼지** · 70/55/45), 세 축 칸
    에서는 '제한적'(판정에 **얼마나 반영할지** · 80/60/40)이었다. 둘 다 맞는데 기준을 안 적어
    모순으로 읽힌다 — 문턱은 어느 쪽도 안 바꾸고 두 질문을 한 줄로 이었다(제안된 *"한 기준으로
    통일"* 은 안 했다 · 어느 쪽으로 맞추든 손으로 고른 새 문턱이다 · §2). 그리고 **'핵심 합리적
    범위'는 그 이름이 아니었다** — 유효 모델 **출력 분포**의 분위수이고, 중심값은 확장 구간
    (10~90) 안으로만 되돌리므로 **25~75 밖에 놓일 수 있다**(모델이 하나면 한 점이다). 이름을
    '모델 출력 25~75분위'로 좁히고 밖일 때·한 점일 때를 적는다. 업황 조정은 규칙집이
    `apply_to_fair_value = 0` 이라 **항상 0** 이고 그러면 ②축(수개월)은 ①축(수년) 중심과 **같은
    수**다 — 사실대로 적는다. 덤: §103 의 *"세 축이 서로 다른 값이다"* 는 픽스처가
    `market_adjustment_pct = -2.0` 을 넣어서 통과하고 있었다(**운영은 그 값을 만들 수 없다**) —
    조정이 있을 때와 없을 때를 둘 다 재고, 없을 때는 화면이 같다고 말하는지까지 본다.
    `docs/RESULT_R243_TWO_NAMES.md` · §260
  → **고침이 판정자 한 명에게만 갔다** (라운드 246 · 2026-09-09). 병렬 감사 6축(에이전트 54개 ·
    적대적 검증)이 42건을 실재로 확인했고, 그중 화면이 거짓을 말하는 넷을 고쳤다. ① 라운드 193 이
    *"결론 문장은 중앙 판정이 낸다"* 로 고친 자리가 **상세 화면**이었고 **추천 카드**는 안 옮겨졌다 —
    카드가 `next_action` 의 headline·kind·conditions 를 직접 읽어, 중앙 판정이 막은 종목도
    `buy_now` 를 보고 **초록으로 승격**하고 "지금 분할매수할 수 있습니다"를 적었다. ② 시장 카드
    기본값에 **박아 둔 숫자**(환율 1,421.00 · S&P500 7,381.87/+65.72/+0.90%)가 있어 네 개의
    `except: pass` 중 무엇이 걸려도 그대로 나갔다 — 같은 함수에서 KOSPI·KOSDAQ 만 'N/A' 였다.
    ③ `(v or 0) / 1e8` 이 미수신을 0 으로 바꿔 `na='미산출'` 을 **도달 불가**로 만들었다(거래대금
    '0억원'). ④ 게이트 설명이 **통과 여부와 무관하게** '기준 통과'를 적었다. 값·문턱 불변.
    **결론을 쓰는 소비자를 먼저 세고 나서 고친다** — 이 저장소에서 같은 결론을 그리는 자리는
    최소 셋이다(추천 카드 · 종목 상세 · 관심종목 행). `docs/RESULT_R246_CARD_AND_NA.md` · §262
  → **가드가 그날 축적을 버렸다 — 순서가 규칙 밖에 있었다** (라운드 247 · 2026-09-09).
    라운드 245 가 갈라진 원장을 잡게 고친 신선도 검사가 2026-09-08 실행에서 제대로 걸렸는데,
    그 단계가 **업로드 앞**이라 2시간 16분치 축적이 통째로 사라졌다(라운드 71c 의 *"쌓기는
    쌓았는데 저장이 안 됐다"*). 이 워크플로는 계보 감사에 대해 **맨 뒤에 둔다 · 축적이 인질로
    잡히지 않게**를 이미 적어 두고 있었다 — 신선도 검사만 그 규칙 밖이었다. 옮겼다(여전히 실패시킨다).
    그리고 라운드 245 가 미뤄 둔 나머지 파일을 합쳤다: 화이트리스트 213개 전수 대조 결과
    **바이트 동일 147 · 로컬에만 54 · 다름 12 · 클라우드에만 0**, 그중 실제로 새 줄이 있던 것은
    다섯이다(`bar_paths_s0` +1,203 · `entry_anchors_s0` +1,203 · **`forward_registry` +300** ·
    `news_events` +32 · `predictions` +289). 전방 기록부는 로컬이 **60행 하루치**였고 클라우드에
    360행 6일치가 있었다 — 11-16 재평가 표본이 6배가 됐다. 로컬을 그대로 올렸다면 축소 가드도
    못 막았다(기준선이 60이라 60→120 을 '늘어난 것'으로 본다). `predictions` 만 열쇠
    `(ticker, date)` 로 합쳤다 — 줄로 합치면 같은 추천이 두 번 기록돼 규약이 깨진다.
    `improvement.db` 는 로컬이 상위집합(클라우드에만 0)이라 로컬 채택. `data-20260909` 로 올려
    다음 자동 실행이 이어받는다. `docs/RESULT_R247_SNAPSHOT_MERGE.md` · §263
  → **재고 나면 화면 문구도 같이 바뀐다** (라운드 250 · 2026-09-09). 라운드 248 이
    시장 판을 올리며 *"매크로가 판정을 개선하는지는 **아직 재지 않았습니다**"* 를 화면에
    적었고, 하루 뒤 라운드 249 가 **쟀다.** 그 순간 그 문장은 거짓이 됐다 — 값·문턱·판정은
    하나도 안 바뀌었지만(매크로는 여전히 표시 전용) **이유가 바뀌었다.** *"안 쟀다"* 와
    *"재 봤는데 근거가 없다"* 는 사용자에게 전혀 다른 말이다. 근거는 **표본 수와 날짜**로
    적는다(번호는 안 쓴다 · R227). 하나만 고치지 않고 유도 모듈 38개의 화면 문자열을 훑어
    *아직·예정·미측정·재지 않* 30건을 하나씩 읽었다 — 값이 없을 때의 상태 문구 23 · 날짜가
    붙은 연구 주장 3 · 날짜 없이 **사유가** 붙은 것 4, 전부 오늘도 참이라 안 고쳤다.
    **참인 문장을 굳이 고치지 않는다.** 곁들여 R246 ③(`(v or 0)/1e8` 이 미수신을 0 으로
    만들어 '0억원'을 냈다)의 형제를 AST 로 훑었다 — 잡힌 16건 중 **화면까지 가는 것은 0건**
    (0 이 소비자에서 `or None` 으로 되돌려지거나, 멈출 때 기본값이 **차단** 쪽이거나,
    가중평균이 분자·분모에 같은 값을 쓰고 분모 0 을 막는다). 검사로 박제하지 **않았다** —
    16건 중 무엇이 괜찮은지는 소비자를 봐야 알고 그 목록은 손으로 적으면 낡는다(R114).
    그리고 **재기만 하고 안 고친 것**: 자동 축적의 `|| true` 단계가 9개인데 신선도 검사가
    이름을 대는 산출물은 2개다(원장·예측은 `snapshot_guard` 가 따로 지킨다). 남는 자리는
    `calibration.json`·`universe_pin_forward.json`·`premarket_history.jsonl` 과, 축적
    루틴이 아예 안 만드는 셋(`sample_audit`·`effective_n_icc`·`sector_perf`)이다.
    **고치려면 그 워크플로를 다시 만져야 하고 R247 이 거기서 2시간 16분치를 잃었다** —
    단계 순서를 먼저 적고 별도 라운드에서 한다. `docs/RESULT_R250_STALE_CLAIM.md` · §265
  → **미뤄 둔 산식 물음을 쟀다 — 지울 근거도 유지할 근거도 못 세웠다** (라운드 251 ·
    2026-09-09). 사용자: *"산식 9월 8일 근처에서 개선한다고 한거 아냐? 전수조사해서 개선방향
    찾아."* 맞다 — 라운드 238 큐 7건 중 다섯은 끝났고 **고정 보정 0.98 제거**만 날짜가
    없었다(나머지 하나는 신뢰도 구성요소 표시 · `confidence_breakdown` 을 엔진이 내는데
    화면이 안 읽는다 · 다음 라운드). 사전등록을 먼저 쓰고(판정 세 갈래를 **미리** · R44
    선례는 게이트에 안 닿는 표시 보정이었으므로 **그대로 옮기지 않는다**) 비용 순으로 쟀다:
    R0 커밋 2건·문서 264개 어디에도 근거 없음 → R1 겹치지 않는 부분집합에서 층화 1,494건을
    **켜고·끄고 두 번** 돌려(사이에 윈저화·클립이 있어 되나눠서는 안 된다 — 실제로 최소
    −15.6% 가 나왔다) **구역 9.8% 바뀜**, 전부 '더 싸 보이는' 쪽 → R2·R3 Δ(끈−켠)
    train **+0.29** · valid **−0.46** · blind **+0.60**, 방향이 어긋나고 CI 가 0 을 제외하는
    구간은 블라인드 하나(**날짜 22** < 하한 30 · R213 의 그 모양) → **(다) 현행 유지.**
    코드 불변. **표본 백필이 열렸다** — R215 가 *"축적 전체(118시간)"* 라 접은 그 길이
    실측 케이스당 0.76~1.70초라 1,494건 × 2판 = **38분**이었고, R215 가 앞으로 쌓이는
    행에 남기게 한 적정가 숫자는 251,528행 중 **3행**(셋 다 2017년 기준일)이라 기다려서는
    안 열린다. 곁들여 **R0 판별식이 첫 판에 틀렸다** — '본문이 이 값을 설명하는 커밋 1건'이
    라운드 238 자신(근거가 *없다고 적은* 커밋)이었다. 낱말 판정을 갈래 판정으로 고쳤다.
    `docs/PREREG_R251_FIXED_HAIRCUT.md` · `docs/RESULT_R251_FIXED_HAIRCUT.md` · §266
  → **전수조사 82건 중 내가 확인한 넷만 고쳤다** (라운드 252 · 2026-09-09). 병렬 조사의
    검증 단계 131개가 사용량 한도로 죽어 결과의 '반박됨' 24건은 **검증이 안 돈 것**이었다 —
    미검증을 고친 것처럼 적지 않는다(§9). ① `repair_ocr_code` 를 **직접** 부르는 두 경로
    (CSV 가져오기 · OCR 텍스트)가 실재 KRX 문자코드 296개 중 **114개(38.5%)** 를 남의
    종목으로 바꿨다 — 라운드 189 의 가드가 `_read_code_token` 한 곳에만 있었다(R246 "고침이
    판정자 한 명에게만"). 기본 함수가 실재 코드 모양이면 손대지 않게 했고(R120e) 색인에서
    **유도한** 296개로 심어서 잰다. ② 화면의 *"Blind/OOS 미구현이므로 모델검증 0점"* 은
    거짓 — 구현돼 있고 매 종목 돈다. 항목 값·가중치를 엔진이 내고 화면은 읽는다(R236).
    ③ 회귀 §3 의 *"Blind 미구현이면 추천 불가"* 는 `False <= X` 라 **실패할 수 없는 검사**
    였고 그 깃발(`BLIND_TEST_IMPLEMENTED`)은 엔진이 읽지도 않는다 — 게이트가 실제로 읽는
    `blind_test_status` 로 바꿨다. ④ 전방 추적 확정 169건 · 기준일 33개 중 **9개가 토·일**
    이고 일요일 픽은 토요일 픽의 복사본이었다 — 수집자 하나가 33 을 보고 *"뉴스 축 하한
    충족"* 이라 적었는데 휴장일을 빼면 **거래일 24 · 미달**이다. 휴장일 판정을 한 곳에
    두고, 동결이 건너뛰며(R222 의 미래 기준일과 같은 자리), `tally` 가 거래일 고유 기준일
    수를 낸다. 행은 안 지웠다. 뿌리(휴장일에 리포트를 만드는 것)는 별도 라운드.
    곁들여 이 파일의 *"39절 등은 아직 맨몸"* 은 R219 뒤로 거짓이었다 — 고쳤다.
    `docs/RESULT_R252_CENSUS_FOUR.md` · §267
  → **전방 판정 기록이 긴 축적 뒤에 있어 22거래일 중 16일을 잃었다** (라운드 253 ·
    2026-09-10). 11-16 재평가가 읽는 `forward_registry.jsonl` 에 지난 22거래일 중 **6일**만
    있었다(`predictions` 는 21일 — 사람이 PC 를 켠 날이 섞인 수). 빠진 16일은 실행이
    실패하거나 시간을 다 쓴 날과 일치한다 — 09-09 실행은 '전방 구간 집중 축적'(첫 수확이
    시작된 날 · 직전엔 9초)에 머물렀고, 작업 한도 300분을 그 단계가 먹으면 업로드 전에
    죽어 그날 것이 통째로 사라진다(R71c·R247 의 그 모양). 기록기(10분)가 98분+α 뒤에
    있었다. **축적 앞으로** 옮기고(동결 해시 기록 바로 뒤) 긴 두 단계에 실측 기반 시간
    예산(120·100분 · `continue-on-error` · 체크포인트로 이어 돈다)을 줬다. 08-19~09-01 의
    열 날은 **다시 만들 수 없다** — 재구성 갈래가 규모는 만들지만 실시간 입력은 못
    되살린다. 첫 수확일 문서의 *"09-07"* 은 원리적으로 그날 0건이다(실제 첫 수확은 09-09
    실행) — 박제 대상이라 안 고치고 결과 문서에 적었다. `docs/RESULT_R253_RECORDER_FIRST.md` · §268
  → **R84 를 사전등록 그대로 다시 쟀다 — 20/20 정보 없음** (라운드 254 · 2026-09-10).
    라운드 84 는 날짜 하한(구간별 30 · 날짜당 10종목)을 valid 21 · blind 14 로 못 채워 **미측정**
    이었고 *"11-16 이후 다시 잰다"* 고 적었다. 그런데 조건은 날짜가 아니라 **표본 하한**이었고,
    원장 251,528행에서 그것이 찼다(valid **45** · blind **34**). 한 글자도 안 고치고 다시 돌렸다
    (R110 선례). 합계 점수는 train t **−0.9** · valid **+2.7** · blind +0.9 — 부호가 어긋난다.
    상위5 짝비교(R49·R110)·검정력(R113)에 이어 **날짜 계열 IC 로도 정보가 없다.** 유일하게
    세 구간이 한 방향인 것은 `vol20`(전부 음 · train t −8.5)인데 valid·blind 는 t −0.7·−0.6 —
    *"못 봤다"이지 "없다"가 아니다*, 문턱은 안 내린다. `demark_bear` 는 blind 에서 기대와
    **반대**로 유의(+2.9) — 단독 반대 유의는 먼저 의심한다(R148). **8월엔 잣대를 못 댔고,
    오늘은 댔더니 없었다** — 두 문장은 다르다(R113). 바꾼 것 없음. `docs/RESULT_R84_RERUN_R254.md` · §269
  → **일일 규칙이 재검토일 전의 이슈를 '오늘의 수'로 닫았다** (라운드 256 · 2026-09-10).
    `model|vb_gap`(재검토 **11-16** · 목표 "같은 국면끼리 5%p 이내")이 09-03 에 전체 구간
    괴리 < 10%p 로 자동 '해결'됐고, `usability|signal_rate`(재검토 09-16 · 목표 "블라인드
    약세 표본 100건+ 뒤 재판정")는 08-15 에 신호율 6.7% > 5% 로 닫혔다. **닫는 규칙과
    이슈의 목표가 다른 것을 잰다** — 화면 '주요 이슈'가 11-16 을 기다리는 과제를 '해결
    완료'로 보였다. 고침은 기본 함수 한 곳(`issue_tracker.resolve_by_key` · R120e · 라운드
    172 의 "닫는 길이 넷"에 다섯째를 안 만든다): 재검토일이 아직 안 온 이슈는 닫지 않고
    사유를 돌려주며 일일 규칙이 그것을 찍는다(§3). 곁들여 **이름이 잰 것과 달랐다** —
    '매수 신호 발생률'은 매수권(58점+) 케이스 비율(6.7%)이고 매수 추천 자체는 251,528건 중
    56건(0.022%)이다. 이름을 잰 것에 맞췄다. 이미 닫힌 두 행은 되돌리지 않았다(등록부 이력을
    손으로 고치는 것은 사람의 결정). R64 §6 의 '거짓 음성 원장'은 전방 기록부(매일 상위 60 전부 ·
    360행 중 HOLD 355)가 **부분 대체**한다 — 새 파일을 만들지 않는다. `docs/RESULT_R256_ISSUE_REVIEW_DATE.md` · §271
  → **09-15 결정을 화면·문서·재측정으로 준비했다** (라운드 257·258 · 2026-09-10). ① 버전 칩의
    '다음에 다시 보는 시점'이 scoring 축에만 붙어 있었다 — 룰북 축의 열린 이슈(손절 조이기 노출
    결정 · 재검토 **2026-09-15**)를 잇는다(날짜는 이슈에서 읽는다). ② '모델 검증 반영' 절의
    *"국면별 엔진 제한 … 미구현"* 은 거짓 — 국면 게이트로 구현돼 있다(전방 재평가 대상). 가중치
    조정 문장은 등록부에서 읽어 만든다(닫히면 문장도 바뀐다 · R250). ③ 일정 문서 둘에 09-04 뒤의
    사실(R249 0/9 · R251 (다) · R254 20/20 · R253 · R252)을 적었다. ④ 결정 재료
    `docs/DECISION_R257_STOP_TIGHTEN_EXPOSURE.md` — 손절 0.6배의 실측을 라운드 21 사전등록
    그대로 다시 쟀다(경로 있는 매수권 6,085건): 평균 손실 **약 −35%**(blind −9.54 → −6.22) ·
    목표도달률 **−10~−15%p** · 검증 기대값 +0.01 → −0.35. **어느 배수도 세 구간을 만족하지
    못한다 — 기각 그대로.** 선택지로 노출할지는 사람이 09-15 에 고른다(대가를 같은 카드에 적는
    조건). 산식·기본값 불변. §272
  → **축적 루틴이 한 번도 안 만들던 산출물 셋을 클라우드가 만들고 신선도 검사가 본다** (라운드
    259 · 2026-09-10). 라운드 217 이 *"여덟 배치 동안 한 번도 갱신 안 됨"* 이라 적은 셋(표본 감사 ·
    유효표본 ICC · 업종 성적)과 라운드 250 이 *"재기만 한"* 신선도 범위(2개). 비용부터 쟀다 —
    생성기 셋 합쳐 **약 30초**. 세 생성기가 신선도 규약(`ledger_rows`)을 따르고, 워크플로가
    셋을 만들며(축적 **뒤** · 업로드 **앞** · `|| true`), 맨 뒤의 신선도 검사가 **다섯**을 본다.
    클라우드에서 실제로 만들어지는지는 다음 실행이 증명한다 — 실패해도 축적은 남는다(R247).
    `docs/RESULT_R259_FRESHNESS_SCOPE.md` · §273
  → **참인 문장도 옆 문장 없이는 거짓으로 읽힌다** (라운드 260 · 2026-09-10 · 문서 전용).
    전수조사(R252) 잔여 넷: ① *"커버리지 97.1%"* 에 분모가 없어 원장 전체로 읽혔다 — 실제는
    개발 구간 매수권 판정완료 **98,822행**(R233 의 규칙 · 같은 이름의 수가 둘이면 단위를 옆에).
    ② R111 절이 상대화를 *"11/16 이후 후보"* 라 적는데 바로 아래 R112 가 16개 전부 기각했다 —
    원문은 두고 한 줄로 잇는다(그때는 참이었다). ③ 레이더가 Cross-sectional Ranking 을 **'연구
    예정'** 이라 적었는데 계획이 없다 — 전제(같은 날 전 종목 스냅샷)가 원장 격자에 없어
    **보류 · 전제 미충족**. 사전등록은 안 썼다(표본 전의 사전등록은 R192 의 그 모양). ④ R216 은
    사전등록만 있고 결과 문서가 없어 *"재지 않은 사전등록"* 으로 보였다 — 표 안의 실측(R1 미달 ·
    미측정)을 결과 문서 자리에 옮겼다. **새로 잰 것은 없다.** `docs/RESULT_R260_DOCS_BATCH.md` · §274
  → **만들었다고 적었는데 운반이 없었다** (라운드 261 · 2026-09-10). 라운드 259 가 *"업로드
    앞 — 만든 것이 올라가야 화면에 닿는다"* 라 적었는데, 업로드 화이트리스트는 `.portfolio/`
    한 뿌리뿐이고 관측 산출물 다섯은 전부 `data/` 에 쓰인다. 클라우드의 다섯은 **신선도 검사를
    통과한 직후 컨테이너와 함께 사라졌고**, 화면이 읽는 git 의 `data/` 는 사람이 손으로 만든
    날(09-02 · 09-03 · 09-08 · 09-10)에만 움직였다 — 라운드 102 가 붙인 FN/FP·취약구간
    지도도 08-10 부터 같은 처지였다. 되받는 쪽의 zip-slip 가드도 `.portfolio/` 밖을 전부
    거부해 담아도 못 풀었다. 고침: 목록은 **신선도 검사 목록에서 유도**(검사가 보는 것 =
    실어 나르는 것 · 손 목록 아님) · zip 의 두 번째 뿌리 `data/` · 가드는 목록 안의 이름만
    연다(개인 자료 패턴 · 목록 밖 json · `..` 거부) · 되돌림은 `ledger_rows`(어느 원장에서
    만들었나)로 막는다. 워크플로는 안 고쳤다. **배포 화면까지의 마지막 한 걸음(git 커밋)은
    여전히 사람**이다 — 원장 동봉본과 같은 규칙. "올라간다"는 문장을 쓸 때는 **어느 목록이
    그것을 고르는지**를 본다. `docs/RESULT_R261_ARTIFACTS_NOT_SHIPPED.md` · §275
  → **감시가 재는 모집단이 이슈가 말하는 모집단이 아니었다** (라운드 262 · 2026-09-10). 라운드
    256 은 닫는 쪽(재검토일 전엔 안 닫는다)만 고쳤고 **여는 쪽**은 그대로였다 — 검증-블라인드
    괴리 감시가 전체 구간(매수권 밖 포함)으로만 재서 **5.4%p < 10** 이라 해소를 내는 동안,
    이슈의 목표이자 화면 머리 수인 매수권(60점+)은 **14.8%p**(69.0 vs 54.2 · n 1,297·1,168)
    였다. 문턱 10%p 는 그대로 — 모집단을 하나 더 재고 어느 쪽이든 넘으면 열며 요약이 두 수를
    같이 말한다(R233). 표본 30 미만이면 그 모집단은 판정하지 않는다. 그리고 §182 가 *"vb_gap 이
    원장에 열려 있지 않다"* 를 잠그고 있었다 — 살아 있는 등록부의 상태를 못 박은 것(R213)이라
    화면 불변식만 남겼다. 국면별 잔여 격차(이슈의 목표 그 자체)는 11-16 의 일이다.
    `docs/RESULT_R262_VBGAP_POPULATION.md` · §276
  → **`|| true` 뒤의 침묵은 실행 기록으로 잡는다** (라운드 263 · 2026-09-10). 일일 개선
    파이프라인의 `pipeline_runs` 가 **14행 · 고유 날짜 6일** — 08-02~09-07 평일 26일 중 **24일
    기록 없음**이고 남은 6일은 전부 사람이 이 PC 에서 돌린 시각이다. 클라우드 단계는 매 평일
    도는데 `|| true` 라 죽어도 초록불이었다(라운드 250 이 센 아홉 중 하나). 파이프라인은
    시작하자마자 `running` 행을 커밋하므로 행이 없으면 **시작조차 못 한 것**이다 — 09-02·09-04
    는 워크플로가 "성공"한 날이다. 원인(그날 단계 로그)은 이 세션이 못 읽었다 — 다음 세션이
    잡는다. 검사 하나(`scripts/pipeline_run_check.py` · 오늘 시작한 행이 success 인가 · 휴장일
    제외 · 못 읽으면 미측정)를 워크플로 **업로드 뒤**에 붙였다(R247) — 이 PC 에서 돌리니 오늘
    기록 없음으로 실제로 붉어졌다. 단계 16 의 `|| true` 는 그대로 — 축적을 인질로 잡지 않는다.
    `docs/RESULT_R263_PIPELINE_RUN_CHECK.md` · §277
  → **이득 상한은 이미 가진 자료로 먼저 센다 — 정밀분석 확대의 상한은 0 이었다** (라운드 264 ·
    2026-09-10 · 문서 전용). 라운드 188 이 비용(종목당 1.0초 · 120종목 약 2.7분)을 재고 *"이득
    상한만 재면 된다"* 고 적은 채 열흘 — 전방 기록부가 매일 **상위 60 전부**에 중앙 판정을
    남기므로 6~60위의 매수 판정 수가 곧 상한이다: **6거래일 360행에서 매수 판정 0**(상위 5 안도
    0). 조건부 후보 +25행은 점수 59·58 로 그날 상위 5 와 **동점의 형제**들이고(R110 의 경계 동점),
    5위 점수와 동점인 행이 하루 9~32행이다. 확대하지 않는다 — 이득 0 인 변경에 연구 표본이
    바뀌는 비용(R188·R164)을 치를 이유가 없다. 61~120위는 자료가 없어 **추론**이라고 적었다.
    곁들여 조건부 후보 40행의 `missing` 칸이 전부 비어 있다(어느 조건인지 기록부만으로는 모른다 ·
    11-16 박제 대상이라 지금 안 바꾼다). `docs/RESULT_R264_DEEP_SCAN_UPSIDE.md` · §278
  → **비어 있는 값의 사유가 원장에 없으면 그 결측은 영영 '모른다'다** (라운드 265 · 2026-09-10).
    원장 `win_rate`(엔진의 20일 자기유사 관측 승률)가 **93,621/251,528 = 37.22%** 비어 있는데
    사유 칸이 없다. 연도로는 갈린다(2015 78.9% → 2026 28.0% · 옛 기준일일수록 빈다) · 자산·국면·
    점수대로는 안 갈린다(매수권 58+ 도 35.2%) · 그리고 `eff_sample` 로는 **설명이 안 된다**(20건
    이상 230,808행 중 79,021 이 비어 있다) — 그 칸은 전략 백테스트 표본이고 `win_rate` 가 비는
    조건은 20일 유사패턴 **매칭 건수**다(라운드 38 이 갈라 둔 두 수). 엔진은 매 케이스 사유
    (`sample_tier` · `match_count`)를 내는데 원장 행이 안 옮겼다. 코드의 None 경로는 하나(표본
    하한 미만)라 사유는 **추론**이지 측정이 아니다 — *"표본 부족 37.22%"* 로 바꿔 적지 않는다(§3).
    앞으로 쌓이는 행에 `sim_match_count` · `sim_tier` 를 남긴다(없으면 None). 기존 행 백필은
    축적 전체라 안 한다(R215 의 자리) — 옛 행의 사유 분포는 층화 표본 백필(R251 의 길)로 추정할
    수 있고 그것은 사전등록 거리다. `docs/RESULT_R265_WIN_RATE_REASON.md` · §279
  → **하한이 실제의 절반이면 하한이 아니다** (라운드 266 · 2026-09-10). §6 의 회귀 하한 2,700 은
    실행 수 4,927 의 55% 였다 — §158 이 파일 중간에서 "자기가 도는 시점까지의 수"(2,796)와 견줬기
    때문이고, §6 자신이 *"하한을 올릴 거면 §158 을 파일 뒤로 옮기는 것이 먼저다"* 라 적어 둔 채
    스무 라운드가 지났다. 검사의 45% 가 조용히 사라져도 넘는 하한이었다(R233 의 '안 돈 두 절'을
    못 잡았다). 옮기고(검사 내용 불변 · 절 안의 이름은 절 안에서만) 하한을 **4,800** 으로. 새 절은
    이제 §158 **앞**에 넣는다. `docs/RESULT_R266_REGRESSION_FLOOR.md` · §280
  → **"못 봤다"를 "보니 없다"로 바꾸는 데 필요한 것은 축적이 아니라 날짜였다** (라운드 267 ·
    2026-09-10). 사용자: *"이 업데이트는 한번은 되야 하는 거 아냐?"* 라운드 251 의 (다)는 valid 26 ·
    blind 22 **날짜**(하한 30)의 한계였고, 날짜는 축적을 기다려도 안 는다 — 겹치지 않는 부분집합의
    기준일은 이미 원장에 있었다. 판정 기준은 한 글자도 안 바꾸고 표본만 valid·blind 기준일 **전부**
    (87 · 47)로 넓혀 다시 쟀다(시드 267 · R251 과 독립 · 1시간 안). Δ(끈−켠) train **−0.055**
    [−0.16, +0.03] · valid **−0.061** [−0.24, +0.06] · blind **+0.016** [−0.93, +0.81] — 날짜
    287·81·38 로 하한을 채웠고 **효과가 0 근처**다. R251 의 블라인드 +0.60(CI 0 제외)은 날짜
    22 → 38 에서 **사라졌다** — R213 의 그 모양이 또 재현됐다. → (다) 현행 유지 · 코드 불변.
    사용자 물음의 답: 이 상수는 **바꿀 것이 없다** — 근거 없음(R0) · 지우면 구역 9.8% 이동(R1) ·
    그래도 판별력은 ±0.15%p 안에서 불변(R267). 남는 것은 *"근거 없는 상수를 효과 없이도 지울지"*
    라는 **정책 결정**이고 그것은 사람이 한다(지우면 매수권 통과 수가 는다 — 개선이 아니라 노출의
    변화 · 하더라도 11-16 전 배포 금지). `docs/PREREG_R267_HAIRCUT_DATE_FLOOR.md` ·
    `docs/RESULT_R267_HAIRCUT_DATE_FLOOR.md` · §281
  → **전수조사는 상태표가 있어야 끝난다** (라운드 268 · 2026-09-10 · 문서 전용). 라운드 252 의
    82건이 gitignored 초안에만 있어 무엇이 닫혔는지 아무도 셀 수 없었다. 상태표
    `docs/CENSUS_R252_STATUS.md` — **완료 50 · 부분 4 · 닫음(이미 잰 것) 2 · 남음 26**. 완료는 결과
    문서 번호로만 가리키고(미검증을 완료로 안 적는다 · §9), 요약 수는 표를 세어 낸다. 남은 26 의
    갈래: 사용자 결정(09-15 · −2% 정책) · 11-16 이후 · 사전등록 거리(재료 있음) · 수집 인프라 ·
    화면(서버 확인) · 문서. §282 가 82건 빠짐없음 · 합계 일치 · 완료의 근거 · 종목 코드 부재를 잠근다.
  → **열리지 않는 관문에 시세를 다시 받지 않는다** (라운드 269 · 2026-09-10 · 문서·검사). 차트
    구조 사전등록(R214)의 R1(같은 날 매수권 ≥6 종목인 날짜 ≥30)이 "레벨 백필(R2 · 수 시간)이
    먼저"로 서 있었는데, 그 표의 오른쪽 열을 다시 읽으니 **원장 전체로 넓혀도 blind 25일** —
    백필해도 미달이다. 라운드 267 과 반대 모양: 거기서는 날짜가 원장에 있었고 표본 설계가 안
    밟았지만, 여기서는 **날짜가 원장에 없다.** 날짜를 더하는 것은 전방 구간뿐 → 11-16 뒤 그
    사전등록 그대로 연다(레이더에 줄). 곁들여 R171 의 업종조정 *'열림 · 지금'* 에 "아직 안
    열렸다" 를 추기했고(열린 것과 하고 있는 것은 다르다), 메타 라벨 랩이 원장의 **상수 피처**
    (뉴스 3칸 · 리플레이 폴백)를 자료로 찍게 했다(손 목록으로 지우지 않는다). 상태표 갱신:
    완료 54 · 부분 4 · 닫음 3 · 남음 21. §283
  → **"페이지가 없다"와 "주소가 옮겨졌다"는 다른 말이다** (라운드 270 · 2026-09-10). 이날 오후부터
    네이버가 옛 종목 페이지(`finance.naver.com/item/main.naver`)를 새 사이트(`stock.naver.com`)로
    302 시켜, 실시세 파서가 **모든 종목**을 라운드 204 의 '종목 페이지 없음'으로 읽고 이름에
    `<title>` 조각을 넣었다 — 회귀 §17 이 오전엔 통과하고 오후엔 죽어서 알았다. 앱의 전 종목 분석과
    클라우드 축적(리플레이도 실시세 삼중 확인을 먼저 부른다)이 막히는 사고다. 새 사이트의 JSON
    (`m.stock.naver.com/api` · basic·integration·finance/annual·업종 목록)이 옛 페이지의 칸을 전부
    주므로 **옛 표식이 없을 때만** 그 길로 같은 `info` 를 채운다(순수 함수 · 펀드·결측·추정치 제외를
    심어서 잰다 · §284). 값을 지어내지 않는다 — 빈 칸의 뜻은 옛 파서 그대로. 옛 파서는 남긴다(되살아날
    수 있다). **같은 날 다섯 경로가 한꺼번에 넘어갔다** — 실시세 · 유니버스(시가총액 목록 → 종목만 ·
    ETF 제외 · R164 경계) · 종목명 검색(자동완성 · 시장을 읽어 붙인다) · 업종 목록·구성(pageSize 상한이
    있어 totalCount 까지 넘긴다) · 외국인·기관 수급(투자자 추세). 회귀가 **10건 실패로 하나씩** 드러냈다 —
    한 경로를 고치면 다음 경로가 보인다. 그리고 폴백 유니버스가 부채비율 `None` 에 `TypeError` 로
    죽었다(`meta.get("debt", 0)` 은 키가 None 으로 있으면 기본값을 안 쓴다). **외부 자료원의 주소가
    바뀌면 사유 낱말이 먼저 낡는다** — 라운드 165 가 사유를 갈라 둔 덕에 로그가 "메인으로 넘어감"을
    말했고 그 말이 답이었다. 돌고 있는 앱은 재시작해야 이 길을 탄다. 회귀 4,954건 · 실패 0 · 건너뜀 0.
    `docs/RESULT_R270_NAVER_REDIRECT.md` · §284(15건)
  → **로그 한 줄이 수집을 죽였다 — 프로브는 인코딩을 데운 채 돌고 있었다** (라운드 271 · 2026-09-11).
    R270 코드를 앱으로 띄우자 사이드바가 '시총 1위 미수신'을 찍고 그 `None` 이 `resolve_symbol` 에서
    앱을 죽였으며, 시장 스캔은 유니버스 폴백 print 에서 `UnicodeEncodeError` 로 죽었다. 뿌리는 하나 —
    R270 이 넣은 **성공 로그** `[Universe] KOSPI — …` 의 em-dash 가 streamlit 자식의 cp949 표준출력에서
    예외를 내고, 그것이 `except Exception` 에 잡혀 **'수집 실패'** 로 읽혔다. 프로브·회귀는 첫 줄에서
    stdout 을 utf-8 로 다시 열어 못 봤다(R167 의 '차가운 프로세스'를 인코딩은 데운 채 돌렸다). 고침은
    한 곳(`_stdout_tolerant` · 인코딩 불변 · 못 찍는 글자만 대체)이고 §285 가 cp949 자식 프로세스로
    **양방향** 심는다. 곁들여 회귀에 없던 옛 경로 셋(시총 1위 · 배당 · 관심도 순위)을 같은 방식으로
    옮겼고, 순매수 상위 목록은 새 사이트에 끝점이 없어(주소 71개 · 번들 218개) **미수신**으로 두고
    사유를 싣는다(전원 0 · 필터 아님). **로그 한 줄이 수집을 죽일 수 있으면 그것은 로그가 아니라
    게이트다.** `docs/RESULT_R271_NAVER_REDIRECT_REST.md` · §285(10건)
  → **25단계 '성공'에 쌓인 것이 0 이었다** (라운드 272 · 2026-09-11). 라운드 263 이 못 읽은 그날 단계
    로그를 열었다 — 2026-09-10 클라우드 실행(main = R260 · 네이버 이전 **뒤** 21:47 KST)은 초록불
    25/25 에 증분 전부 +0: 전방 판정 기록 0건(유니버스 수집 실패 → 폴백 19종목 전부 '페이지 없음') ·
    케이스 축적 120분 시간초과 · 경로 기록 0행(실패 574). 09-08(가드 실패) · 09-09(수동 실행 둘 취소)
    도 비어 전방 기록부는 **09-07 에서 멈춰 있다**(360행 · 6일 · 다시 만들 수 없다 · R253). 라운드 81 의
    증분 요약은 원장의 0 을 정상으로 두는 것이 맞지만(격자가 차면 0) **전방 기록부는 거래일마다
    반드시 는다** — '증분 0' 의 뜻이 파일마다 다른데 한 요약이 같은 규칙을 대고 있었다. 오늘 행이 0 인
    거래일을 실패로 만드는 검사를 워크플로 맨 뒤(업로드 뒤 · R247)에 뒀다. main 이 R270+ 가 아니면
    다음 실행이 처음으로 붉어진다 — 그게 맞다. `docs/RESULT_R272_ZERO_DAY_GUARD.md` · §286(6건)
  → **같은 식인 두 줄은 설명이 그것을 말하고, 사슬은 사슬이라고 말한다** (라운드 273 · 2026-09-11 ·
    상태표 #9·#78). 게이트 표의 '진입 깊이 현실적' 과 '보유기간 안 도달 가능' 은 판정식이 글자까지
    같은데(R246 이 자리 때문에 안 합쳤다) 화면은 다른 것을 재는 것처럼 보였다 — 설명 끝에 "위 줄과 같은
    규칙(σ 상한 하나)"을 붙였다(이름·자리·조건 수 불변). 실패 원인 분류는 return 사슬이라 앞 가지가 뒤
    가지를 먹는데(valid 의 '방향 오판' 44.71% 는 BEAR 가 4건뿐이라 흘러간 몫) 화면은 나란한 원인 표로
    냈다 — 채점기가 순서를 산출물(`failure_chain`)에 싣고 화면은 "처음 맞는 하나에만 세어진다"를
    말하며 순서는 읽기만 한다(§4 · 손으로 안 적는다). 문턱·이름·순서 불변. 상태표 완료 56 · 부분 4 ·
    닫음 3 · 남음 19. `docs/RESULT_R273_SAME_RULE_AND_CHAIN.md` · §287(5건)

실제 사고: 라운드 30에서 GS 진입 92,011원에 손절 100,775원(위), NAVER
진입 214,733원에 목표 172,895원(아래)이 나갔다. 라운드 25에서 표시
진입가만 바꾸고 손절·목표를 옛 값에 남겨 둔 탓이다.

## 5. 디자인

Koyfin·TradingView 급 금융 터미널. 미니멀·고대비·플랫.

- **이모지 금지.** 아이콘은 `ui_kit._ICONS`의 Lucide SVG를 쓴다
- 타이포는 10단 스케일 고정, **최소 12px** (§77이 잠근다)
- 색은 상승 빨강 / 하락 파랑 (한국 관행). 원색 남발 금지
- 카드는 `ui_kit.reco_card` 하나로 통일 — 한 화면에 두 종류 금지
- 사이드바는 아코디언. **'종목 찾기'만 항상 펼침**(`always: True`),
  나머지는 접힘
- WCAG 대비는 브라우저에서 **실측**한다. 알파 합성과 배경 그라디언트를
  같이 봐야 한다

## 6. 검증

```bash
C:/Python314/python.exe test_pipeline_fixes.py
```

4,800건 이상 (2026-09-10 기준) — 그날 전체 실행은 4,927건 ·
실패 0 · **건너뜀 0** 이었다. **전건 통과가 기본**이다.
⚠️ 종전 하한은 2,700 이었고 *"일부러 보수적"* 이라 적혀 있었다 — §158 이
파일 **중간**에 있어 자기가 도는 시점까지의 실행 수(2,796)와 견줬기 때문이다.
그러면 검사의 45% 가 조용히 사라져도 하한은 넘는다(R233 의 '안 돈 두 절'을
하한이 못 잡았다). **라운드 266 이 §158 을 요약 블록 바로 앞으로 옮겼다** —
이제 하한은 전체 실행 수와 견준다. 새 절은 여전히 요약 블록 앞에 넣되
**§158 앞**에 넣는다(§158 뒤면 그 절의 수가 하한 대조에서 빠진다 · §280).
하한을 올릴 때는 이 줄의 수와 날짜를 같이 갈고, 실행 수가 그보다 크게 줄었으면
검사가 사라진 것이니 하한을 내리지 말고 원인을 찾는다.
전체 규모는 요약 줄(`실행 N건 · 실패 M건 · 건너뜀 K건`)이 말한다.
요약은 실행·실패·**건너뜀**을 함께 찍는다 — 건너뜀은 통과가 아니다.
그리고 '0건'을 주장하는 스캐너 검사는 `check(..., scanned=N)` 으로
**몇 개를 봤는지** 말한다. `scanned=0` 은 통과가 아니라 실패다
(§225 가 AST 로 강제한다 · 라운드 194). 실패하면 원인을 고치지, 검사를
지우지 않는다. 다만 검사가 옛 구현을 요구하고 있다면 검사를 현실에
맞춘다 — 그때는 왜 바뀌었는지 주석으로 남긴다.

⚠️ **새 절은 요약·`sys.exit` 블록 앞에 넣는다 — 그 블록이 파일 끝이어야 한다** (라운드 233 ·
2026-09-07). §249(R232 · 19건)와 §250 을 파일 **끝에** 붙였는데, 요약과 `sys.exit(0)` 이 §248
뒤에 있어 **두 절은 전체 회귀에서 한 번도 안 돌았다.** 사전 점검은 절을 마커로 떼어 돌리므로
초록불이었고, 전체 회귀의 실행 수가 안 움직인 것이 유일한 단서였다(4,522 → 4,522). 요약
블록을 끝으로 옮겼고, 그 블록이 **자기 뒤에 절 머리가 있는지 스스로 본다**(있으면 실패 —
"그 절은 안 돌았다"). **실행 수가 새 절만큼 안 늘면 그 절은 안 돈 것이다.** 같은 모양:
R195 의 "존재는 실행이 아니다" · R194 의 "0건인지 못 봤는지".

⚠️ **전체 회귀 전에 `scripts/preflight.py` 를 먼저 돌린다** (라운드 230). 바뀐 .py 컴파일 +
최근 절·자주 걸리는 절을 떼어 초 단위로 돌린다(앞 절의 이름이 없으면 '의존', 건너뜀은
'미측정'으로 따로 센다). 이 세션에서 전체 회귀(10분)가 배경에서 **여섯 번** 소스 리터럴
문제로 실패했다 — 새 절이 잠근 리터럴이 두 조각으로 갈렸거나, 옛 절이 이번에 바꾼 낱말을
잠갔거나. 전부 실시세 없이 잡히는 것들이다. 순서: 고침 → 사전 점검 → 전체 회귀 **한 번**.
전체 회귀가 여전히 최종 판정자다.
⚠️ **사전 점검이 '정의보다 먼저 쓰는 이름'을 본다** (라운드 255 · 2026-09-10). 한 세션에서
두 번, §3 에 넣은 새 검사가 §148 에서야 정의되는 `_read148` 을 써서 **컴파일은 통과하고
전체 회귀가 37건에서 NameError 로 죽었다** — 40분 뒤에야 알았다. 절 드라이런은 그것을
'의존'(앞 절의 이름)으로 분류해 통과시켰고, 같은 판별식을 가진 회귀 §226 은 그 줄보다 **뒤**라
못 봤다. 이제 사전 점검 ①' 이 `scripts/usebefore_audit.scan` 을 초 단위로 부른다(판별식은
한 곳 · 베끼지 않는다). 심어서 잡히는 것을 확인했다(`433:_read148`).

⚠️ **회귀는 앱 서버를 내리고 돌린다** (라운드 218 · 하루에 두 번 당했다).
회귀 3절·39절은 **실시세를 실제로 받아 온다**(`load_bitemporal_data` ·
`run_full_pipeline`). 앱 서버가 같은 네이버 엔드포인트를 두드리고 있으면
`The read operation timed out` 이 나고 회귀가 통째로 죽는다 — 같은 코드가
서버를 내리고 돌리면 4,309건 완주한다. 코드 문제가 아니다.
R218 이 3절에 재시도·건너뜀을 넣었고, **라운드 219 가 실시세 호출 자체를**
`_retry_live` **로 감쌌다**(`test_pipeline_fixes.py` 3절 머리 · 한 번 재시도 후
건너뜀 · `docs/RESULT_R219_LIVE_FETCH_RETRY.md`) — 종전 이 줄은 *"39절 등은
아직 맨몸"* 이라 적고 있었고 그것은 R219 뒤로 거짓이었다(라운드 252 · 2026-09-09).
중단돼도 요약은 찍힌다(`atexit` — 마지막 절과 여기까지의 수를 적는다).

화면 결함은 **서버를 띄워야 나온다.** 회귀 전건 통과·렌더 무예외
상태에서도 화면이 스스로 모순되던 사례가 여러 번 있었다.

```bash
C:/Python314/python.exe -m streamlit run web_app.py --server.port 8512
```

**2026-08-16(라운드 114) — 회귀만 믿으면 안 되는 이유가 또 나왔다.**
화면을 눈으로 보다가 둘을 잡았고, 둘 다 회귀가 초록불이었다.

- 같은 5종목을 **두 경로로** 세고 있었다. 목록은 `verdict_core` 의
  bucket/actionable(대기 1·뺌 4), 요약 칸은 스캐너의 옛 `cat` 문자열
  (0·0·5). 합계만 같고 내역이 달랐다 — §4 가 금지한 그것이다
- **§110(이모지 금지)이 49개를 놓치고 "0건"을 찍고 있었다.** 한 검사에서
  구멍이 **세 군데** 나왔고, 하나를 막을 때마다 다음 것이 드러났다:

  | 무엇이 좁았나 | 발견 계기 | 놓친 수 |
  |---|---|---:|
  | **범위** — U+2300-23FF 블록 누락 | 화면의 `⏳` | — |
  | **판별** — 줄 단위 독스트링 건너뛰기 | 화면의 `💡` | **35** |
  | **대상** — 손으로 적은 파일 10개 | import 그래프 유도 | **14** |

  판별이 가장 컸다. `"""` 개수를 세는 방식은 이 저장소의 화면 문자열
  (`st.markdown(f"""…""")`)을 **전부 독스트링으로 봤다.** 범위를 넓혀도
  그 줄들은 애초에 안 보고 있었다. 대상도 같은 모양이었다 — web_app 이
  닿는 모듈은 30개인데 10개만 적어 뒀고, `report_generator`(레포트 제목)
  ·`leakage_guard`(면책 고지)가 영원히 검사 밖이었다.

  셋 다 고쳤다: 계열 통째 범위 · AST 판별 · import 그래프 유도.
  그리고 **이모지를 심어 두고 검사가 그걸 실제로 잡는지** 확인하는
  자기검사를 붙였다 — 0건이 '없다'인지 '못 봤다'인지 구분되게.

→ **검사가 무엇을 재는지 이름만 보고 믿지 않는다.** 0건을 낸 검사는
  "그럼 있을 때는 잡느냐"를 심어서 확인한다. 그리고 검사의 **대상 목록을
  손으로 적지 않는다** — 손으로 적은 목록은 반드시 낡는다.

**2026-08-17(라운드 120c~e) — 유도로 바꿨는데 그 유도가 좁았다.**
같은 결함(마크다운 별표가 화면에 글자 그대로)을 호출부에서 **세 번**
고쳤고 세 번 다 다른 자리였다: 킷의 `note()` → web_app 의 `_md_safe` →
엔진 note 의 f-string 직접 보간. 8곳 → 3곳 → 1곳 → 그리고 **또 1곳**.

네 번째를 못 찾은 이유가 더 나쁘다. 라운드 114에서 "대상을 손으로 적지
않는다"고 import 그래프 유도로 바꿨는데, 그 유도가 `node.module` 만 보고
있어 **`from improvement import issue_ops` 를 안 따라갔다.**
`improvement/` 아래 5개 파일이 §16·§77·§110 검사 밖에 있었고, 그중 한
곳의 산문이 화면에 별표 그대로 나가고 있었다. 게다가 그 그래프 탐색이
세 절에 **거의 글자까지 같게 베껴져** 있어 구멍도 셋이 똑같았다.

→ 유도는 `scripts/lineage_audit.reachable_modules()` **한 곳**에만 둔다
  (`import a.b` · `from a import b` · `from a.b import c` 전부 따라간다).
  유도 결과의 **개수를 찍는다** — 30개 → 35개처럼 숫자가 움직이면
  그동안 안 보던 것이 있었다는 뜻이다.

→ 그리고 **호출부를 하나씩 고치지 않는다.** 같은 결함을 두 번째 고치고
  있다면 공통 진입점을 찾고, 없으면 만든다. 여기서는 킷의 기본
  이스케이퍼 `_esc()` 가 굵기를 복원하게 하고 속성 자리만 `_esc_attr()`
  로 갈랐다 — 호출부 70곳이 한 번에 닫혔다. 기본값을 고를 때는
  **빠뜨렸을 때 나는 실패가 어느 쪽인지**를 본다: '별표가 사용자에게
  노출'보다 '속성에 태그가 들어감'이 드물고 눈에 띈다.

**2026-08-24(라운드 167·170) — ETF 펀더멘털 적정가: 낼 수 있는 만큼만 냈다.**
사용자 요청으로 ETF 구성종목을 룩스루해 적정가를 냈다(R167 · 1,161종목
100% 수집 · 사전등록 R1~R4 통과 · **148개 · 표시 전용**). 산식은
**배수를 만들지 않는 형태**로 썼다 — `적정NAV ÷ NAV = Σ w_i ×
(적정가_i ÷ 현재가_i)`. 좌수·설정단위가 식에 없어 못 아는 값을 안 쓴다.

R170 이 나머지 1,013개가 **왜** 안 되는지 전수로 갈랐다:
- **577개(49.7%)** 해외주식·채권·스왑 — **재료가 없다.** 사용자의 두 ETF
  (`480020` 은 스왑만, `0040Y0` 은 미국주식+국고채)가 여기다. 안 된다
- **239개** — 조인은 되는데 구성종목 **161/1,015(15.9%)** 의 적정가가 없고
  그중 **153개(95%)가 "멀티플 모델 적용 범위 밖"** 이다. 삼성전기(−89% ·
  PER 172배) · 알테오젠(−94%) · LG에너지솔루션 · 삼성바이오로직스 …

→ **천장은 ETF 자료가 아니라 우리 단일종목 적정가 모델이다.** 국내 테마
  ETF 는 정확히 그 종목들로 채워져 있어 한 종목이 막으면 통째로 막힌다.
  그 거부는 **옳다** — −89% 짜리 적정가를 내면 그게 지어낸 값이다.

→ **넓히려던 방법은 기각했다.** 비중이 없을 때 주식수×가격으로 유도하는
  방법을 사전등록하고 **답을 아는 409개에서 먼저 검증**했다. R1(중앙
  0.0019) 통과, **R2(95분위 0.02027 > 0.02) 미달**, **R4(6개 < 10) 미달**.
  0.02027 은 문턱을 1.4% 넘긴 값이라 *"사실상 통과"* 라고 쓰고 싶은
  자리였다 — **그러지 않았다.** §205 가 문턱 리터럴과 기각 사실을 함께
  잠근다. 근거: `docs/RESULT_R170_ETF_CEILING.md`

**2026-08-23(라운드 164) — 판별식이 한 경로에만 살아 있었다.**
사용자가 두 ETF(`0040Y0` · `480020`)를 못 찾았다. 원인은 하나였다 —
이 저장소가 종목코드를 **`\d{6}` 으로만** 읽는데 KRX 는 문자 섞인 6자리
코드를 발급한다(네이버 ETF 1,161종목 중 **296종목 · 25.5%**).
그 식이 **14군데에 손으로** 적혀 있었다.

더 나쁜 것은 **규칙이 이미 저장소 안에 있었다**는 점이다.
`portfolio._read_code_token` ③ 가지가 `[0-9][0-9A-Z]{5}` 를 쓰고 주석에
`ETF '0040Y0'` 을 예로 적어 두었는데, **OCR 경로에서만** 살아 있었다.
검색·관심종목·조회는 그 규칙의 존재를 몰랐다.

→ 새 규칙을 만들지 않고 그것을 `stock_code.py` **한 곳**으로 올렸다
  (§2 재사용). 호출부 여섯 자리가 그것을 부르고, §201 이 조회·검색 함수
  안에 `\d{6}` 이 되살아나는지 **AST 로 함수 본문만** 본다.

→ **넓힐 때 어디까지인지 먼저 적는다.** 유니버스 스크래핑과 관심종목
  발굴은 **일부러 안 넓혔다** — 넓히면 ETF 가 추천 스캔 모집단에 들어가
  **연구 표본이 바뀐다.** 라운드 129 가 참았던 자리와 같다. 검사가 그
  경계를 못 박는다(넓히면 실패한다). 넓히려면 새 사전등록이다.

→ 그리고 **화면 기능 요청이 결함을 데려온다.** "ETF 도 검색되게" 라는
  요청 하나를 파 보니 §3 위반(남의 코드 생성)과 라운드 119 가 걷어낸
  "실패하면 삼성전자"가 지표 쪽에 그대로 남아 있었다.

→ **그 유도가 또 좁았다 — 이번이 세 번째다.** 회귀가 새 모듈 둘을
  "검사 대상에 없다"고 잡았는데, 원인은 `reachable_modules()` 가
  `git ls-files` 로 **추적 중인 파일만** 봤다는 것이었다. 즉 **새로
  만든 파일은 커밋하기 전까지 §16·§77·§110 밖**이고, 정작 검사가 가장
  필요한 것이 새 파일이다. `--others --exclude-standard` 를 더해 아직
  add 하지 않은 파일도 보게 했다(gitignore 된 `_probe/` 는 여전히 제외).
  **35개 → 37개.** 라운드 120e 가 "개수를 찍으라" 한 이유가 이것 —
  숫자가 안 움직이면 그 창은 아직 닫힌 것이다.
  근거: `docs/RESULT_R164_STOCK_CODE_ETF.md`

**2026-08-24(라운드 165) — 하루 만에 뚫렸다. 같은 결함의 다른 표기.**
164 가 검색 경로를 고치고 화면까지 확인했는데, 사용자가 `0040Y0` 을 열자
앱이 죽었다(`일봉 수신 실패`). **그 메시지가 거짓말이었다** — 네이버는
`0040Y0` 으로 **325봉을 정상 제공**한다. 우리가 코드를 망가뜨려 물었다:

```python
clean_symbol = ''.join(filter(str.isdigit, symbol))   # '0040Y0.KS' → '00400'
```

전수로 훑으니 **네 자리**(일봉·배당·뉴스·공시)였다.

→ **§201 이 못 잡은 이유 둘 다 내가 하루 전에 어긴 것이다.**
  ① `\d{6}` **표기 하나만** 찾았다 — `filter(str.isdigit)` ·
  `re.sub(r'\D')` 는 같은 뜻의 다른 표기다.
  ② 검사 대상을 **함수 두 개로 손으로 적었다** — 터진 넷은 전부 그 밖.

→ 그래서 §202 는 **표기가 아니라 idiom** 을 막고(*코드에서 숫자만 뽑기*),
  **모든 모듈**(37개)을 유도로 훑고, **심어서** 잡히는지 확인한다.
  판별(`code.isdigit()`)은 오탐하지 않게 갈랐다.

→ **사유를 섞지 않는다.** 예외 하나가 "코드를 못 읽었다"(우리 문제)와
  "시세를 못 받았다"(상대 문제)를 같은 문장으로 말하고 있었다. 그 문장
  때문에 처음에 네이버를 의심했다.

→ 그리고 **화면이 스스로 모순됐다** — ETF 적정가 문구가 "NAV 미연동"이라
  적는데 바로 옆 칸이 NAV 를 띄우고 있었다(164 가 연동했다).

→ **검사가 사용자 자료를 바꾸고 있었다** (§9). 164 가 '최근 본 종목'을
  파일에 남기게 했더니 회귀의 AppTest 렌더가 돌 때마다 테스트 종목
  (SK하이닉스·다날·CJ ENM)이 사용자 목록에 쌓였다. 프로브가
  `GAEUM_NO_LOCAL_WRITE` 를 켜고 화면이 그때 **쓰기만** 멈춘다 —
  **읽기는 그대로 둔다**(읽기까지 끄면 검사가 빈 화면을 본다).
  그리고 그 검사는 **값으로** 잰다: 회귀 시작 시점에 `.portfolio/` 세
  파일의 수정시각을 찍어 두고 끝에서 안 바뀌었는지 본다.
  근거: `docs/RESULT_R165_ETF_BARS.md`

## 7. 버전

`versioning.py`가 5축(model·scoring·rulebook·schema·news)을 따로 센다.
버전은 앱 출시일이 아니라 **그 축이 마지막으로 바뀐 시점**이다.

```python
versioning.release(axis='model', kind='gate', reason='...', note='...')
```

`axis`와 `kind`를 헷갈리지 않는다 (`schema`는 **축**이지 종류가 아니다).
커밋 제목 끝 괄호의 버전은 실제 이력에 있어야 한다 — 회귀가 검사한다.
`scripts/gen_update_history.py`를 돌려 업데이트 이력을 갱신한다.

## 8. 터미널·경로

- 복합 쉘 구문·`sed`/`grep`/`tail` 파이프·`2>&1`·`python -c` 인라인 금지
- 검증 코드는 `.py` 파일로 먼저 쓰고 단일 명령으로 실행한다
- 프로브는 gitignored `_probe/`에 둔다
- `AppData\Local\Temp`, `HWAN-9~1` 8.3 경로 금지

## 9. 보안·정직성

- 네이버 계정·쿠키·CAPTCHA 우회 금지
- 보유종목·평단가·자격증명 커밋 금지. **포트폴리오를 외부 API/LLM에 보내지 않는다**
- 평균 매수가는 **보유 판단에만** 쓴다 (예측·적정가·점수에 미반영)
- 성과를 좋게 보이게 쓰지 않는다. 우위가 없으면 없다고 적는다

**현재 서 있는 결론**: 이 엔진의 매수권 신호에는 실전에서 재현되는
비용 차감 우위가 없다. 목표 배수(0.4R~3.0R)도 진입 엔진(10종)도 세 구간
모두에서 양수가 아니었다. 화면과 문서는 이 사실을 감추지 않는다.

**그리고 라운드 49에서 더 근본적인 것이 나왔다 — 순위에 정보가 없다.**
같은 날 안에서 점수 상위 5종목과 6위 이하를 158일치 짝지어 비교했더니
상위5가 **적중 −2.29%p · 비용후 EV −0.389%p** 로 오히려 나빴다
(이긴 날 72 · 진 날 85 · 부호검정 z=−1.04 로 유의하지는 않다).
TOP1 55.7% · TOP3 55.7% · TOP5 55.8% 로 **단조성도 없다.**

**2026-08-16(라운드 110) 재측정 — 8배 표본에서도 같다.** 원장이 2만 →
18만 건이 된 뒤 사전등록을 **한 글자도 안 고치고** 다시 돌렸다:
개발 구간 166,132건 · 기준일 2,609일. R1~R4 **전부 미달**이고 표본
조건 R5 만 통과했다 — **표본 부족 탓이 아니었다.**
날짜를 맞춰 짝지으면(810일) **적중률 차이 중앙값이 정확히 0.0%p** 이고
부호검정 z=−1.39 로 여전히 유의하지 않다. 합계로는 미추천이 나아 보이나
날짜 집합이 다르므로(미추천 810일 vs TOP5 2,609일) 그 차이는 과장이다 —
**"순위가 해롭다"가 아니라 "정보가 없다"가 맞는 말이다.**
곁들여, 2,609일 중 **926일(35.5%)에서 상위5 경계에 동점**이 있었다.
근거: `docs/RESULT_R49_RERUN_R110.md` · `scripts/rank_value_r49.py`

**라운드 111 — 어디서 사라지는지도 쟀다. 재료에도 없다.**
하위점수 7종으로 각각 같은 잣대(같은 날·짝비교)를 돌렸더니 **8개 전부
미달**이다 (가장 나은 `trading_timing` 이 z=+0.72, 보정 문턱 2.73).
그리고 **거칠어서가 아니다** — `strategy_quality` 는 서로 다른 값이 950개인
사실상 연속값인데 z=−1.31 로 오히려 대조군보다 못하다.
→ **가중치 조정은 해답이 아니다.** 재료에 없는 정보를 합산이 만들 수 없다.
지금 7종은 전부 **종목의 절대 상태**를 재고 있고, 그건 같은 날 시장이
공유하는 성분에 묻힌다. 상대화(같은 날 순위·업종 내 상대값)를 재료
단계에 넣는 것이 후보이나 **11/16 이후 새 사전등록으로** 다룬다.
(→ 바로 아래 **라운드 112 가 그 후보를 쟀고 16개 전부 미달로 기각**했다 — 이 줄은
그 전의 기록이다 · 라운드 260 표기.)
근거: `docs/RESULT_R111_SCORE_ANATOMY.md` · `scripts/score_anatomy_r111.py`

**라운드 112 — 그 후보(상대화)도 아니다.** 업종 내 백분위와 자기 이력 대비
z 로 각각 8종, 도합 **16개 전부 미달**(문턱 2.9552, 최고 +2.17).
같은 날 순위·평균빼기는 **정렬 순서를 안 바꾸므로 애초에 시험 대상이
아니다** — 사전등록에 미리 적고 뺐다.
결정적인 것은 대조다: **같은 날·같은 종목을 원값으로** 매겨 보니 상대화의
순수 기여가 **도움 3/8 · 평균 −0.14** 로 동전 던지기였고, **상대화 없이
업종 표본으로 줄이기만 해도 원값 z 가 −0.42 ~ +2.10 으로 흩어졌다.**
→ 표본을 4분의 1로 줄이는 것만으로 ±2 가 움직인다. **+2.17 은 증거가
아니다.** 보정 없는 1.96 을 썼다면 여기서 셋을 '발견'했을 것이다.
자기 이력 대비는 **일관되게 해롭다**(도움 1/8 · 평균 −0.63) — 같은 날
다른 종목과의 비교를 버리는 변환이기 때문이다.
→ **가중치 튜닝 · 분해능 개선 · 상대화 셋 다 후보에서 빠진다.** 원장 안의
재료를 어떻게 조합·정규화해도 같은 날 종목을 못 가른다. 남은 길은 **지금
없는 정보를 넣는 것**이다.
근거: `docs/RESULT_R112_RELATIVIZATION.md` · `scripts/relativization_r112.py`

**라운드 113 — 그 "없다"가 얼마나 강한 말인지도 쟀다. 5%p 미만은 못 본다.**
25개 시험이 다 미달했다고 "정보가 없다"를 적었는데, **"못 봤다"와 "보니
없다"는 다른 말이다.** 실제 원장 구성에 효과를 주입해(합성 데이터 없음)
검정력을 쟀더니 **80% 를 넘으려면 5~8%p** 가 필요했다 — 보정 문턱에서
종합점수는 **8%p**, Δ=3 은 **어느 문턱에서도 0%** 다.
→ 그래서 **두 문장을 함께 쓴다**: ① 이 잣대로 잰 범위에서 순위에 정보가
없다. ② 다만 **5%p 미만의 효과는 이 잣대로 못 본다.** 상위5 기준선이
55.8% 이므로 5%p 는 60.8% — 배제한 것은 **큰 우위**이지 작은 우위가 아니다.
곁들여 **귀무 분포의 중심이 0 이 아니라 −0.22** 였다(검정이 살짝 보수적).
관측 −1.39 는 귀무의 **8.5% 지점 · 양측 경험 p=0.170** 이다 — 라운드 110 의
*"해롭다가 아니라 정보가 없다"* 에 이제 숫자가 붙는다.
그리고 표본이 검정력을 가른다: 날짜 810일 vs 1,329일에서 Δ=5 검정력이
**10% vs 92%** 다. **보정은 옳지만 공짜가 아니다** — 문턱을 올릴 거면
표본도 같이 키운다.
근거: `docs/RESULT_R113_POWER.md` · `scripts/power_r113.py`

**여기서 내 규율 결함 셋도 같이 적는다.** ① 표본 조건을 **종목 수로만**
걸었더니(B4) 34.8% 로 통과했는데 정작 **날짜가 65% 날아갔다**(810 → 286).
표본 조건에는 **케이스와 날짜를 함께** 넣는다. ② Bonferroni 문턱을
**반올림**해 적어(2.9552 → 2.95, 2.7344 → 2.73) 문턱을 조금 내렸다.
판정은 더 엄한 쪽으로 다시 했고 결과는 같았다. 앞으로 **올림**한다.
③ 사전등록의 `Δ=0` 행을 "거짓양성률 검사"라 적었는데 그 행은 난수가 없어
**재현 검사**였다. 거짓양성률은 결과를 섞은 귀무 분포로 **따로** 쟀다.
셋 다 같은 모양이다 — **검사가 무엇을 재는지 이름만 보고 믿었다.**
고친 방식도 같다: 기준을 낮추지 않고, 재려던 것을 따로 제대로 쟀다.

**2026-08-19(라운드 129) — "미래 말고 과거 표본을 늘리면?" 에도 답을 냈다.**
전방 재평가(11/16)를 기다리는 대신 원장을 키우자는 물음이라 자원 배분
문제다. 세 축을 갈라 쟀다.

- **종목 축은 이미 포화다.** 종목을 줄여 곡선을 그리니 첫 372종목이
  짝비교 **461일**을 벌고 마지막 373종목이 **14일**을 벌었다(33배 차).
  케이스는 늘지만 **이 잣대가 쓰는 표본(날짜)은 거의 안 는다.** 종전의
  "+80종목 확장"들이 늘린 것은 케이스였다.
- **날짜 810일의 절반은 데이터가 아니라 잣대 구조다.** 6종목 이상인
  1,623일 중 **813일**이 탈락하는데, 전부 그날 매수권이 **5종목 이하**라
  상위5를 떼면 대조군이 빈다 — **상위5가 자기 대조군을 먹는다.**
  다만 이걸 근거로 규칙을 넓히지 않는다(결과를 본 뒤의 재분석).
  게다가 **넓힌 잣대는 이미 재 봤다** — 라운드 84 가 하루 전 종목 IC 로
  합계 점수 **−0.0027 · t −0.3** 을 냈다. 방법을 바꿔도 같은 답이다.
- **과거를 끝까지 채워도 한 칸이다.** 필요 날짜를 정확히 계산하면
  (R112 보정) 8%p 546일 · **5%p 2,051일** · 4%p 4,379일 ·
  **3%p 15,148일** · 2%p 785,736일이고, 1%p 는 방향 자체가 없다.
  물리 상한은 246일/년 × 14.9년 = **3,681일**(지금의 4.5배)이므로
  **볼 수 있는 하한이 5%p → 4%p 로 한 칸 내려갈 뿐**이다.
  3%p 는 상한의 4.1배(62년치), 2%p 는 3,194년치다.

→ 그런데 **R110 의 관측 점추정은 0.0%p** 다. 하한이 5%p 든 4%p 든
  관측값은 거기서 한참 아래다. **표본을 늘리는 것은 "못 봤다"를 줄이지
  "없다"를 뒤집지 않는다.** 남은 길은 R112 가 지목한 그대로 —
  **지금 없는 재료를 넣는 것**이다(`docs/CANDIDATES_R124_EVENT_ENGINES.md`).

**여기서도 검사가 먼저 걸렸다.** 필요 날짜를 닫힌 식으로 내고 R113
시뮬레이션과 대조했더니 괴리가 **19.9%p**(허용 8%p)였다. 둘이 다른 것을
재고 있었다 — R113 은 *"이 810일에서"*(날짜 고정), 식은 *"이런 날을 N일
모으면"*(날짜 재추출)이다. 물음은 뒤쪽이므로 **기준을 내리지 않고 잣대를
부트스트랩으로 바꿔** 둘을 맞췄다. 고친 뒤 자기검사 셋을 통과했다
(거짓양성률 2.0% · 되짚기 괴리 3.2% · 단조 10→26→66→97%).
근거: `docs/RESULT_R129_SAMPLE_CEILING.md` · `scripts/sample_ceiling_r129.py`

이것이 뜻하는 바는 분명하다 — **진입 타점·트레일링 스탑·켈리 공식 같은
개선을 얹기 전에 점수 산식 자체를 다시 봐야 한다.** 잘못된 목록을 예쁘게
정렬하는 일이 되기 때문이다. 새 기능 요청이 들어와도 이 순서를 바꾸지
않는다. 근거: `docs/PREREG_R49_RANK_VALUE.md`

**2026-08-22(라운드 139~151) — R112 가 지목한 "없는 재료"를 넣어 봤다.
공시다. 끝까지 갔고, 이 엔진에는 쓸모가 없었다.**

수집부터 다시 했다. DART 전체공시가 `selectDate` 로 과거까지 열려
**2,249,225건 · 3,865일 · 2010-12-23~** 를 박제했고(R139~145), 회사명을
종목코드에 붙이는 조인을 먼저 재서 **원장 주식 1,190개 100%** 를 확인했다
(R146). 그 위에서 네 번 물었다.

| 라운드 | 물음 | 답 |
|---|---|---|
| R147 | 주주환원 공시가 **원장 케이스**를 가르나 | z 0.78 — 기각 |
| R148 | 공시 자체에 알파가 있나 (**순수 창**, 7유형) | **양의 알파 0/7** |
| R149 | 기준선 대비로 다시 | 6/7 이 **음**으로 유의 |
| R150 | 그 음이 **구성 효과**인가 | 아니다 — 매칭해도 남는다 |
| R151 | 그 발견이 **원장으로 옮겨지나** | **안 옮겨진다.** 방향도 반대 |

**세 가지를 규칙으로 남긴다.**

**① "0 대비 유의"는 기준선이 0 이 아닐 때 아무것도 말하지 않는다.**
R148 은 7유형 전부 "유의(−)" 를 냈는데, 사후 진단에서 **비이벤트
기준선도 −0.232%** 였다 — 시가 진입→종가 청산 창에 상수 비용을 얹으면
어느 날이든 음이다. 사후 진단이 없었다면 *"공시 직후는 해롭다"* 고 적었을
것이고 그것은 **비용과 창 구조를 이벤트로 오독**한 것이다. R44 의 "방향
반대" 경고에 한 줄을 더한다 — **반대 방향이 유의해도, 먼저 기준선을
본다.** 그리고 R149 는 R148 의 읽기 하나도 고쳤다: 자사주·배당은
**평균은 같고(+0.009%p) 보통 날은 못하다**(중앙 −0.202 · z −4.25).
**평균 하나만 보면 틀린다 — 평균과 중앙을 같이 적는다.**

**② 순수 창의 사실이 곧 엔진의 쓸모는 아니다.** R150 까지의 발견은 참이다
(6/7 유형 · 매칭 뒤에도 z −3.42~−10.37). 그런데 원장에 옮기니 **중앙값이
정확히 0.000%p** 이고 부호마저 양이었다. 이 엔진은 스캔 시점 가격으로
들어가지 **부풀려진 시가를 사는 동작이 없어서** 그 기전이 닿지 않는다
(해석이다 — 측정하려면 새 사전등록이 필요하다). **효과를 발견하면
"우리 진입에서도 그 자리를 밟는가"를 반드시 따로 묻는다.**

**③ 재구현 자기검사를 판정 조건에 넣으면 실제로 걸린다 — 세 번 중 두 번.**
"이전 라운드의 발표 값을 허용 오차 안에서 재현하지 못하면 **측정을
중단한다**"를 사전등록에 넣었다.
- R149 — 걸렸다. **접수일이 거래일이 아닌 공시**(유형당 5~221건)를
  날짜 순회 설계가 통째로 빠뜨렸다. 허용 오차를 늘리지 않고 매핑을 고쳤다.
- R150 — 걸렸다. 그런데 원인이 데이터가 아니라 **내 사전등록의 모순**
  이었다(§2 "20거래일 미만 제외" vs §5 "그대로 재현"은 동시 성립 불가).
  판정 기준은 한 글자도 안 바꾸고 **검사가 같은 표본끼리 견주도록** 고쳤다.
- R151 — 통과했고, **통과가 '없다'인지 '안 봤다'인지 구분되게** 값을
  산출물에 남겼다.

**곁들여, 자유도 유혹이 실제로 왔고 넘어가지 않았다.** R151 민감도에서
달력을 바꾸면 z +2.28 로 문턱 2.25 를 넘었다. 사전등록이 고른 달력이
아니므로 판정에 쓰지 않았고, **§192 가 "문턱 넘은 민감도 칸이 있을 때
문서가 쫓지 않았는지"를 검사한다.** (그 칸도 양의 방향이라 쫓아가도
규칙을 지지하지 않았다.)

→ **매수 우위는 R148·R149·R150·R151 통틀어 0개다.**
근거: `docs/RESULT_R146~R151_*.md` · `scripts/event_*.py`

**2026-08-22(라운드 153~156) — 겹침(R124 §4)도 열어서 끝까지 갔다.
효과는 실재하고 희석 탓도 아니지만, 여전히 매수 우위는 아니다.**

R124 §4 는 겹침을 **마지막**으로 미루며 조건을 걸었다("개별을 먼저
재라 · 개별이 귀무인 곳에서 겹침만 유의하면 다중검정의 산물이다 ·
조합 수만큼 보정을 올리고 **그 비용을 미리 적어라**"). 개별이 끝났으니
열었다.

| 라운드 | 물음 | 답 |
|---|---|---|
| R153 | 겹치면 더 큰가 | **증폭한다** (T2 중앙 −0.661%p · z −4.91) |
| R154 | 그게 **희석(증자) 쏠림**인가 | 아니다 — 증자를 빼도 z −4.06 |
| R155 | "증자 한 건 ≈ 다른 것 두 건"인가 | **못 가린다** (점추정 −0.006%p, TOST 미달) |
| R156 | k≥3 은 **언젠가** 잴 수 있나 | **불가능** — 필요 5,429일 vs 상한 3,665일 |

**규칙으로 남기는 넷.**

**① 조합별 시험은 비용을 먼저 계산하고 포기했다.** 7유형의 2중 조합
21가지 → 보정 문턱 z 3.04 인데 **가장 흔한 조합조차 975건**으로 표본
조건 1,000 을 못 넘는다. 그래서 조합이 아니라 **개수 k** 로 물었고
시험은 둘뿐이다. R124 가 요구한 *"비용을 미리 적어라"* 의 답이 이것 —
**적어 보니 못 하는 일이었다.**

**② "차이가 유의하지 않다"는 "같다"가 아니다.** R155 는 동등성
주장이라 보통 검정으로는 증명이 안 된다. **TOST** 로 적극적으로 물었고
마진은 **R154 가 이미 발표한 값**(공시 한 건어치 = 0.170%p)을 썼다 —
새 숫자를 만들지 않았다. 결과는 **점추정 −0.006%p(마진의 3.5%)인데
동등성은 못 세움.** 그리고 필요 날짜 5,693~7,580일 vs 상한 3,665일 —
**이 마진에서는 앞으로도 못 세운다.** 마진을 넓히면 통과하지만
**하지 않았다.**

**③ 자기검사를 판정 조건에 넣으면 계속 걸린다 — 이번엔 네 라운드에서
다섯 번.** "앞 라운드의 발표 값을 재현 못 하면 **중단**"을 매번 걸었다.
- R153 — **한 번에 둘**: 대조군 정의(다른 유형 공시가 대조에 섞임) ·
  배수 계산(행마다 세어 부풀림)
- R156 — **닫힌 식 버그**: 양측 검정인데 `p−0.5`(단측)를 써서 음의
  방향을 "영원히 못 닿는다"로 처리. **부트스트랩이 안 잡았으면 그대로
  나갔다.**
- 그리고 고친 뒤에도 **159일 vs 333일(괴리 109%)** 이 남았는데 이건
  버그가 아니라 **내 사전등록이 검정력을 명시하지 않아 덜 정해진**
  탓이었다(R150 과 같은 모양). 규칙대로 **부트스트랩 채택**.

**④ 벽이 우리 규칙일 때도 규칙을 넓히지 않는다.** R156 에서 k≥3 을
막은 것은 검정력이 아니라(333일이면 되고 지금 190일) **우리가 채택한
표본 조건 1,000건**(5,429일)이었다. 드문 부분집합에 그 조건의 모양이
안 맞는다는 뜻이지만, **결과를 본 뒤라 넓히지 않았다** — R129 가 같은
자리에서 참은 것과 같다.

→ **매수 우위는 R148~R156 통틀어 0개다.** 순수 창에는 사실이 쌓였지만
(공시 뒤 D+1 시가 매수는 못하고, 겹치면 더 못하다) **원장에는 닿지
않는다**(R151 z +2.06/+1.57 기각 · R153 z 0.75) — 이 엔진은 부풀려진
시가를 사는 동작이 없다.

**남은 길**: 실적 이벤트(2026-11-16 이후) · 업종조정(업종 매핑 필요) ·
**진입 시점 자체를 재는 것**(순수 창의 효과가 어느 진입에서 사라지는지).
근거: `docs/RESULT_R153~R156_*.md` · `scripts/event_confluence_r153.py` ·
`scripts/confluence_dilution_r154.py` ·
`scripts/dilution_equivalence_r155.py` · `scripts/k3_ceiling_r156.py`

**2026-08-31(라운드 191~195) — 다섯 라운드, 전부 "같은 말이 두 곳에서 달랐다".**
사용자의 서진시스템 지적에서 시작한 흐름이 여기까지 왔다. 값은 거의
안 바꿨다 — **바꾼 것은 어디서 나오느냐**다.

| 라운드 | 무엇이 두 곳에 있었나 | 실측 |
|---|---|---|
| R191 | 왕복 거래비용이 **0.30 / 0.36 / 0.41** 셋 | 통일하면 통과분 547건 중 **267건(48.8%)** 뒤집힘 → **값 안 바꿈** |
| R192 | 이모지 판별식 **일곱 벌** · 판정 경로 목록 **일곱 벌** · 괴리 잣대 **다섯** | 판별식이 계열 5/7~7/7 못 봄 · 유도하면 14개(신규 68 · 위반 0) |
| R193 | '다음 조건' 칸이 중앙 판정을 안 읽음 | **25종목 중 20(80%) 어긋남**, 그중 11은 `actionable=False` |
| R194 | '0건' 주장에 커버리지 없음 | 스캐너 40개 중 **10개**가 아무것도 못 봐도 통과 |
| R195 | 죽어 있던 §55 계약 둘 | 점추정 단조성 **깨짐** · 그 꺾임은 **잡음**(n=20) |

**규칙으로 남기는 다섯.**

**① 존재는 실행이 아니다.** R191 이 상수에 이름을 붙이며 클래스 몸통에
두고 `self.` 없이 읽어 `NameError` 로 파이프라인이 죽었는데, **내
프로브는 16/16 통과였다** — 상수의 *존재*와 리터럴의 *부재*만 봤고 그
줄을 한 번도 실행하지 않았다. 그리고 그것을 막으려고 넣은 §222 검사도
**첫 판에 틀렸다**(지역 변수 이름을 산출물 키로 씀). 같은 습관이 한
라운드에 두 번이다. → 리팩터링 검사에는 **실행 증거**를 한 줄 넣는다.
그 줄이 만든 키가 산출물에 있는지 보는 것으로 족하다.

**② 판별식도 낡는다 — 대상 목록만이 아니다.** R114 가 이모지 범위를
넓혔는데 **베낀 여섯 벌은 그대로**였다. 고침은 호출부가 아니라 기본값
이고(§120e), 다시 베끼는 것을 **AST 로** 막는다(리터럴이 한 곳뿐인가).

**③ 낱말을 늘리는 것은 답이 아니다.** R194 에서 "심기가 있는가"를 재려다
판별식이 **세 번** 좁았다: '심어' 낱말 → 29개(부풀림) → 낱말 확대 →
12개 → 구조 `len(X)>=N` → 12개 → 맨 이름·임의 수 비교 → 10개.
**판별식이 좁으면 남의 잘못을 부풀린다** — 좋게 쓰는 것만 부정직한 게
아니다. 답은 산문을 그만두고 **인자**로 만드는 것이었다(`scanned=`).

**④ 화면도 판정자다.** R193 에서 중앙 판정은 제대로 막았는데 화면이
하위 모듈(`next_action`)의 문장을 **직접** 그려 "분할매수할 수 있습니다"와
"쫓아가지 마세요"가 한 장에 나왔다. → 결론 문장은 중앙 판정이 내고
(`next_headline`), 하위 모듈은 **재료**만 준다. 통과했을 때는 하위 모듈의
말을 **그대로** 쓰고 막혔을 때만 바꾼다.

**⑤ 재지 않은 사전등록은 없느니만 못하다.** R192 가 사전등록을 쓰고
재지 않았고, R195 가 실행했다. R3 미달·R4 잡음이었고 **기준을 내리지
않았다** — 비단조를 사실로 적고 계약을 *"증명된 역전 없음"*(Wilson ·
새 문턱 없음)으로 다시 썼다. 두 번째 계약(매수권 60점+ 비용 차감 양수)은
재 보니 **거짓**이라 되살리지 않고, 그 값을 매 실행 찍고 **화면이 우위 없음을
말하는지**를 계약으로 삼았다. 그 값은 원장이 자라며 움직인다 — 184,759행일 때
n=13,011 · +0.141% · 차감 후 **−0.409%**, 251,528행일 때 n=16,347 · +0.162% ·
차감 후 **−0.388%**(2026-09-09 · `data/buyzone_ev_r195.json`). **부호는 안 바뀌었다.**
⚠️ 이 '차감'은 R195 의 §55 계약값 **0.55%p** 로 뺀 것이다 — 운영 비용은 0.36
(R191 · `verdict_core.COST_PCT`)이고 그것으로 빼면 **−0.198%**, 부호는 같다. 연구
스크립트에는 비용 상수가 네 값(0.30·0.36·0.41·0.55 · 40건 · 2026-09-09 실측)으로
남아 있다 — 각각 그 라운드의 재현용이라 바꾸지 않는다. **수를 인용할 때 어느
비용으로 뺀 값인지 같이 적는다**(라운드 255).

근거: `docs/RESULT_R191~R195_*.md` · `docs/PREREG_R191_COST_UNIFY.md` ·
`docs/PREREG_R192_BAND_MONOTONE.md` · `scripts/band_monotone_r195.py`

**2026-09-01~02(라운드 196~202) — 사용자 지적 셋에서 시작해, 검사가 무엇을
재는지로 끝났다.** 사용자가 물었다: *"이 종목을 갖고 계신가요도 연동되게
해줘야지 · 종합차트에 분봉·일봉·주봉도 · 산출 불가는 어떻게 해야 해."*
셋 다 고쳤고, 그 과정에서 **결함 다섯**이 더 나왔다.

| 라운드 | 무엇이 | 실측 |
|---|---|---|
| R196 | 원장이 16일째 안 자라는데 문서는 *"매 거래일 자란다"* | 리플레이 원장만 멈춤(실전 추적은 살아 있음) |
| R197 | **내가** 스모크 한 번으로 원장 972건을 지웠다 | 백업에서 복구(184,769) · 가드 셋 |
| R198 | R197 이 만든 틈 — 화면과 문서가 **977건** 차이 | `total_cases` vs `ledger_rows` |
| R199 | 보유 조회가 **접히는 아코디언 안**에 있었다 | 접히면 안 돌아 라디오가 '아직 없음' |
| R200 | 쓰기 금지가 **읽기까지** 껐다 | 회귀 렌더가 늘 **빈 보유목록**을 봤다 |
| R201 | 어제 넣은 봉 버튼이 **안 돌고 있었다** | `let` TDZ → 그 뒤 스크립트 전부 죽음 |
| R202 | 봉은 바뀌는데 화면은 **2016년** | 숨긴 지표가 시간축을 쥠 · 리사이즈가 범위 덮어씀 |

**규칙으로 남기는 넷.**

**① 파생 산출물을 다시 쓰는 작업은 줄어들 수 있는지 먼저 본다.**
`calibration_lab` 은 원장을 매 실행 `open(...,'w')` 로 통째로 쓰는데,
채점 루프가 시세 실패를 `continue` 로 넘긴다 — **네트워크가 한 번
흔들리면 표본이 영구히 작아진다.** 같은 계열이 R71c(60,462→400) ·
R72 로 **이미 두 번** 적혀 있었고, 둘 다 *백업 목록*으로만 고쳐 덮어쓰기는
그대로였다. 고침은 셋: 빠진 건수·종목을 **세어 찍고** · 더는 못 받는
종목은 **종전 채점을 이어받고**(`carried_over`) · 시세 미수신으로 결과가
줄면 **덮어쓰지 않는다**(문턱이 아니라 **사유**로). 복구 도구도 남겼다
(`scripts/restore_graded_from_backup.py`).

**② 화면 전체가 쓰는 값을 접히는 위젯이 만들면 안 된다.** 보유 조회가
사이드바 아코디언(§5 에 따라 기본 접힘) 안에 있어, 접혀 있으면 통째로
안 돌고 `user_entry_price` 가 0 으로 떨어졌다. **위젯은 값을 고치는
자리이지 만드는 자리가 아니다** — §4 의 사촌이다.

**③ 이름이 하나면 읽기와 쓰기가 안 갈린다.** R165 가 *"쓰기만 멈추고
읽기는 그대로 둔다"* 고 적었는데 이름이 `ALLOW_LOCAL_STORE` 하나뿐이라
**주석이 거짓**이었다. 그래서 회귀의 AppTest 렌더는 늘 빈 보유목록을
봤고 — **보유자 경로가 한 번도 검사된 적이 없다.**

**④ 검사에는 층이 있다. 위층이 아래층을 대신하지 못한다.**

| 무엇을 보는가 | 잡는 것 | 못 잡는 것 |
|---|---|---|
| 소스에 글자가 있는가 | 오타·누락 | **실행 실패** |
| 끝까지 도는가 (§222 · §229) | NameError · TDZ · 문법 | **틀린 결과** |
| 결과 상태를 값으로 | 위 전부 | (재는 비용) |

R201 은 봉 버튼을 *글자로* 확인해 통과했는데 브라우저에서는 **눌리지도
않았다**(`let` TDZ 로 그 뒤가 전부 죽음). 고치자 이번엔 스크립트가
멀쩡히 도는 채로 **2016년을 그리고 있었다**(R202). → node 가 있으면
**스크립트를 진짜 돌린다**(`scripts/chart_js_smoke.js` — DOM 은 무엇을
물어도 스텁을 주는 Proxy 라 안 터지고, TDZ·ReferenceError 만 남는다).
그리고 **선언 순서에 기대는 코드를 안 쓴다** — 상태는 DOM 한 곳에서 읽는다.

**곁들여, 내 판별식이 이 세션에서 여섯 번 틀렸다.** 좁아서 못 본 것이
넷(심기 낱말 → 29건 부풀림 · `0개를 재고` · 맨 이름 깃발 · `sum()>N`),
넓어서 오탐한 것이 둘(바깥 `if` 포함 · 저장 패널). **낱말을 늘리는 것은
답이 아니었다** — 산문을 그만두고 **인자**(`scanned=`)와 **구조**로
옮기고, 심기로 **양방향**(잡는가 · 오탐 안 하는가)을 확인한다.

근거: `docs/RESULT_R196~R202_*.md` · `scripts/usebefore_audit.py` ·
`scripts/chart_js_smoke.js` · `scripts/restore_graded_from_backup.py`

**2026-09-02(라운드 211~213) — 축적이 파생물의 판정을 흔들었다. 값을
잠근 검사가 데이터 표류로 깨졌다.** 원장을 손으로 더 키우다가(206,769 →
**250,725**, 세 배치 +43,956 · 이로써 현행 계획 표본을 사실상 소진),
원장을 읽어 매번 재생성되는 사전등록 아티팩트 둘의 중간 측정값이 뒤집혔다.

| 라운드 | 무엇이 | 실측 |
|---|---|---|
| R211 | 6차 축적(+10,000) · R208 블라인드 통과 70→111 | CI [−5.49,+2.11] 여전히 0 포함 · (a) |
| R212 | 7차 축적(+15,000) · R205 `strict_not_worse` **False→True** | 그래도 기각 · 0.36 유지 |
| R212 | R208 블라인드 통과 111→**206** · CI **[−5.55,−0.15]** 0 제외 | 그래도 (a) — train·valid 부호 반대 |
| R213 | 8차 축적(+18,956) · `strict_not_worse` True→**False** · R208 CI 다시 0 포함 | **진동 확인 — 채택 안 한 게 옳았다** |

R213 이 못을 박았다: `strict_not_worse` 가 False→True→False 로 **진동**하고
(R213 은 최대 표본 n=253 에서 0.41 이 다시 나쁘다), R208 블라인드 CI 도
0 제외→포함으로 오르내린다. **R212 에서 뒤집힌 True 를 보고 채택했다면
R213 이 틀렸다고 증명했을 것이다.** 값을 안 잠근 회귀는 이 진동에 안 깨졌다.

**규칙으로 남기는 셋.**

**① 재생성 아티팩트는 값이 아니라 불변식을 잠근다.** R205 회귀 락이
`strict_not_worse is False`(중간 측정값)를 못 박고 있었는데, 자기 주석은
*"숫자는 재검증하지 않는다 — 표류한다"* 라 적고선 값을 잠근 모순이었다.
0.41 게이트 통과분의 블라인드 평균이 축적하며 −5.83→−4.23→−0.80 으로
움직여(n 31→126) 그 값이 뒤집혔고, **회귀가 데이터 표류로 실패**했다.
고침: 규칙(0.36)·판정·**큰 표본 방향**(R3)·표류가 문서에 적혔는지만
잠근다. 판정(`verdict`)은 잠가도 된다 — 그건 바뀌면 사람이 다시 판단해야
하니까(R208 의 `(a)` 락). 중간 측정은 안 된다.

**② 블라인드는 축적 중 비정상(non-stationary)이다.** 축적은 최근 기준일을
더하고 블라인드 구간(≥날짜)은 조성이 바뀐다. 그래서 블라인드 단독 신호가
세지는 듯 보여도(R205 의 0.41 · R208 의 CI 가 0 제외) train·valid 와
어긋나면 **효과가 아니라 조성 변화**다. R208 이 (a)로 선 이유가 이것 —
블라인드만 유의하고 train(+)·valid(+)는 반대다. **세 구간 잣대를 둔
이유가 이것이고, 블라인드 단독으로는 매 배치 무언가 "발견"된다**(§9 ·
R44 의 "방향이 구간마다 뒤집히면 효과가 아니다").

**③ 노이즈 뒤집힘은 기각을 재열지 않는다.** R205 채택은 매수권 신호
48.8% 를 없애는 큰 변경이다. 채택의 입증 책임은 채택 쪽이고(비악화를
*세워야* 한다), 한 배치 뒤집히는 블라인드 지표로는 못 세운다. 큰 표본
(R3 · n=6,131)이 *조일수록 나쁘다* 하면 큰 표본을 믿는다. 뒤집으려면
**새 사전등록** — R4 에 안정성·검정력 가드를 달아 비정상 블라인드에서도
서는 비악화를 요구한다(R113·R129 처럼). 사후에 기준을 내려 채택하지 않는다.

근거: `docs/RESULT_R205_COST_UNIFY.md` §"R4 는 축적하면 표류한다" ·
`docs/RESULT_R208_EV_GATE_ANTISELECT.md` §"다시 쟀다" ·
`test_pipeline_fixes.py` §221

**2026-09-09(라운드 248~249) — 유가·금·금리를 화면에 올리고, 판정에 넣을
근거는 없다고 적었다.** 사용자: *"유가 금 금리 등등을 보면서 할건지 … 엔진들에게도
반영해주고 연구할 수 있도록해줘."*

R248 이 전제 하나를 뒤집었다 — **유가·구리·변동성지수·원달러·S&P500 은 이미
2013-12-31 부터 3,189봉 수집되고 있었다.** 화면 어디에도 **한 판으로** 없었을
뿐이다. 진짜 새것은 금(GC=F)·미국채 10년 금리(^TNX) 둘이고, 이름을 붙이기 전에
실제로 오는지 확인했다(안 오는 XAU/USD·US10YT=X·FRED:DGS10 은 이름을 안 붙였다 · §3).
금리는 **%p**, 색은 **방향**(좋고 나쁨이 아니다), 못 받은 축은 **미수신**.
*"왜 떨어지는지"* 는 **쓰지 않는다** — 두 지표를 나란히 놓는 것까지가 사실이고
잇는 순간 지어낸 값이 된다. *"내일 방어적으로?"* 는 이미 채택된 **국면 게이트**로만
답하고, 안 깎였을 때도 말한다(침묵하면 *'재지 않았다'* 로 읽힌다).

R249 가 그 다음 물음을 **쟀다** — 판정에 넣을 수 있나. **아홉 축 전부 미달.**

| | train | valid | blind | |
|---|---:|---:|---:|---|
| 변동성지수 | **+0.68** (z **+5.17**) | **−1.36** (z **−2.14**) | +2.52 | 방향 어긋남 |
| 원달러 | **+0.38** (z +2.91) | **+1.29** (z +2.01) | −0.67 | 방향 어긋남 |
| 금 | **+0.50** | +1.05 | +1.40 | 방향은 같으나 CI 가 0 포함 |
| 미10년 | −0.06 | −0.64 | −2.87 | 방향은 같으나 CI 가 0 포함 |

(%p · 높은 날 − 낮은 날 · 굵은 것은 CI95 가 0 을 제외 · 나머지 다섯 축도 방향 어긋남)

**train 만 봤으면 두 개를 '발견'했을 것이다.** 변동성지수 train z **+5.17** 은 이
저장소가 잰 어떤 재료보다 크고 보정 문턱 2.78 을 한참 넘는데, **valid 는 반대
방향이면서 유의하다.** 원달러는 train·valid 둘 다 유의한 같은 방향인데 블라인드에서
뒤집힌다 — 셋 중 둘이 맞으면 채택하고 싶어지는 자리이고, 그래서 관문을 **셋 모두**로
미리 적어 두었다. 부호 자체가 직관과 반대인 것도 그대로 적는다(train 에서는 변동성이
**높은** 날의 매수권 신호가 더 좋았다).

**이번엔 표본이 막은 게 아니다.** 라운드 45 의 VIX 축은 *"블라인드 매수권 280건이
실은 고유 기준일 5개"* 라 못 쟀는데, 원장 251,528행에서 **블라인드 고유 기준일 52일**
(하한 30 · R45 재사용)이고 아홉 축 모두 중앙값 가름의 **연속 덩어리가 4~13개**라
'높음/낮음'이 곧 한 시기가 아니다. R0 을 통과하고 나서 떨어졌다 — **미측정이 아니라
재고 미달이다**(라운드 113 의 두 문장).

**재기 전에 내 사전등록을 두 번 고쳤다.** ① *'극단'* 의 정의가 없었다 — 극단을
정하려면 *상위 몇 %* 라는 **새 숫자**가 필요해 §2 위반이므로 **중앙값**으로 갈랐다
(고르는 값이 아니라 자료가 정하는 값). 대신 **중앙값 가름은 극단을 재지 못한다**를
적었다. ② **다중검정 보정이 없었다** — 축이 아홉인데 CI95 만 보면 아무것도 없어도
하나쯤은 0 을 벗어난다(라운드 112 의 그 자리). Bonferroni(9) z **2.78**(라운드 113
규칙대로 **올림**)을 옆에 적고 **판정은 등록한 CI95 로** 했다. 둘 다 R1 을 재기
**전**이고, 결과적으로 필요 없었다 — R3(방향)에서 아홉 중 일곱이 먼저 떨어졌다.

**바꾼 코드는 하나도 없다.** 매크로는 표시 전용이고 화면이 그 사실을 적는다
(*"위 유가·금·금리·환율은 이 판단에 들어가지 않습니다"*). 2026-11-16 전방
재평가 때 다시 센다. **매수 우위는 R148~R156 에 이어 R249 도 0개다.**
근거: `docs/PREREG_R249_MACRO_AXIS.md` · `docs/RESULT_R248_MACRO_BOARD.md` ·
`docs/RESULT_R249_MACRO_AXIS.md` · 회귀 §264·§265

**2026-09-03(라운드 215) — "제일 중요한 건 적정가를 맞추는 것." 원장이 그
편이었고, 그래서 사전등록으로 쟀다. 극단 괴리 가설은 미달.** 이 저장소가
잰 재료 중 블라인드에서 양수였던 것은 적정가 구역(R183 `entry_zone`)뿐이다.
그 표에서 **깊은 할인(안전마진 확보)이 얕은 할인(적정가 이하)보다 나빴다** —
적정가가 극단에서 과대(H1)라면 개선은 새 배수가 아니라 괴리의 수축이다.
원장 250,725행 · 세 구간 · 날짜 군집 부트(시드 215)로 R1 을 쟀다:

| 구간 | n(이하/확보) | 날짜 | 점추정 | CI95 | 판정 |
|---|---:|---:|---:|---|---|
| train | 5,837 / 71,370 | 2,330 | **−0.07**%p | [−0.26, +0.11] | 부호 반대 |
| valid | 550 / 5,725 | 75 | +0.11 | [−0.48, +0.61] | 0 포함 |
| blind | 481 / 3,756 | **46** | +1.17 | [−0.54, +2.61] | **0 포함** |

→ **(a) 현행 유지 · H0.** R183 의 블라인드 간격은 커졌는데도(+0.24→+1.17)
유의하지 않다 — 날짜가 46개뿐이다(행 4,237은 독립 표본이 아니다 · R80·R129).
R2(블라인드 백필 · 수 시간)는 **열지 않았다**(앞에서 미달이면 뒤는 안 잰다).
세 구간에서 방향이 같은 것은 둘뿐이고 둘 다 이미 게이트에 있다: '판정
불가'(적정가 없음)가 train·blind 적중 최하(54.7·49.1 → R183·R185 가
`OUT_OF_DOMAIN` 을 막는다) · '적정가 이하'가 blind 유일 양수(+0.93%).

**남기는 것 둘.** ① 적정가 **정확도**를 재려면 원장에 숫자가 있어야 한다 —
구역 6칸으로는 비율 분위·신뢰도 등급(`price_axes.TIERS` *"원장에 신뢰도가
없어 미검증"*)을 못 잰다. `calibration_lab` 이 앞으로 쌓는 행에
`fair·fair_conf·fair_status·ent_class` 를 남기게 했다(기존 행 백필 = 축적
전체라 안 했다). ② *"산식이 개선이 안 되는데 잘 되고 있는 거 맞지?"* 의 답:
**개선이 안 되는 게 아니라 잰 것마다 근거가 없어 안 바꾼 것이다** — 순위·
하위점수·상대화·공시·비용 게이트·차트·적정가 극단까지 전부. 바꿀 근거가
생기면 바꾼다. 그때까지 안 바꾸는 것이 §2·§9 다.
근거: `docs/PREREG_R215_FAIR_VALUE_CALIBRATION.md` ·
`docs/RESULT_R215_FAIR_VALUE_CALIBRATION.md` · `scripts/fair_value_zone_r215.py` ·
`test_pipeline_fixes.py` §232

**2026-09-03(라운드 216) — "산식은 개선 어떻게 잡고 있어?" 계획을 적는다.**
재료가 같은 날 종목을 못 가른다는 것은 서 있다. **정보가 있는 곳은 시장
수준(국면)** 이고, 원장 250,725행을 점수대×국면×구간으로 갈라 보니 '연습
71.5% vs 실전 51.3%' 의 정체가 **국면 조성**이었다:

| 매수권 58+ | valid | blind | 괴리 |
|---|---:|---:|---:|
| 전체 | 62.6% (8,721) | 55.0% (6,483) | 7.6%p |
| BULL | 59.2% | 53.4% | 5.8 |
| SIDEWAYS | 73.3% | 62.1% | 11.2 |
| **BEAR** | 86.1% (**36건 · 날짜 4**) | 51.8% (1,409) | **34.3** |

검증 구간에 하락장이 거의 없었고 블라인드는 22%가 하락장이다. 그리고
**하락장 블라인드에서 점수는 거꾸로 간다**(40~49점 71% · 58~59점 54% ·
60~64점 44%). **엔진의 실패는 하락장에 몰려 있다 — 개선은 거기를 겨눈다.**

그 자리를 겨눈 후보가 R124 후보 4(반등 확인 · "측정 가능 · 지금")였는데
**한 번도 사전등록되지 않았었다.** 이번에 썼다(`PREREG_R216`). 표본 단위는
케이스가 아니라 **BEAR→회복 에피소드·날짜**인데 R1(하한 30 · R84 재사용)이
**미달**이다 — 에피소드(KOSPI) train 114 · valid **0** · blind **1**
(KOSDAQ 98·3·2), 매수권 BEAR 날짜 valid **4** · blind 17. train 만의
관측(확인 6~20일 구간이 BEAR 진행 중보다 나음)은 가설 생성이고 판정이
아니다(모양이 세 구간에서 같지 않다). **덤 — 원장 `regime` 은 시장별이다**:
시장을 섞은 날짜열로 세면 "하루 한 국면"이 어느 행을 잡느냐에 따라 수가
갈린다(첫 집계 191·3·6 이 그랬다 · 화면은 3·1 로 셌다). 국면 에피소드는
**시장별로** 세고, 화면에는 행 자체의 regime 으로 센 **날짜**를 낸다.

→ **계획**: ① 규칙은 안 바꾼다(바꿀 근거가 없다). ② 화면은 정직하게 —
모델 성적에 국면×구간 표를 표시 전용으로 냈다(§233). ③ 하락장 에피소드는
축적으로 못 만든다 — **새 하락장이 끝나야 하나 는다.** 2026-11-16 전방
재평가 때 R55(국면 라우팅) 판정과 함께 R1 을 다시 센다. 두 국면 규칙을
동시에 넣지 않는다. ④ 그때도 30 미만이면 미측정이라 적고 하한을 안 내린다.
⑤ 적정가는 앞으로 쌓이는 행에 숫자가 남으므로(R215) 그것이 크면 R2 를 연다.
**덤 — "도구 5개(1개 실패)"의 진짜 원인은 rerun 이었다.** 스캔 요약을 셈으로
맞추자(R215) 바로 그 자리가 잡혔다: *상위 5 → 완료 4 · 제외 0 · 누락 1*.
루프의 모든 경로가 결과 아니면 사유를 남기는데도 그랬다 — `q_engine` 이
**모듈 수준에서 매 rerun 새로 만들어지고**, 스캔은 끝나며 `st.rerun()` 을
부른다. 결과는 세션에 남았지만 사유(`last_scan_failures`)는 옛 엔진 객체에
남아, 다음 그리기의 요약은 **새 엔진의 빈 목록**을 읽었다. R37 이 넣은
사유 기록이 그때부터 화면에 닿은 적이 없었다. 고침: 결과와 **같은 곳**
(세션)에 사유를 남기고 거기서 읽는다(§4 — 두 곳에 두면 한쪽이 증발한다).
고친 뒤 실측(2026-09-03 · 새 세션): *상위 5 → 완료 4 · 제외 1 — 예: 20일 평균 거래대금 17.0억원 < 20억원*. 누락 0. 즉 그 "1개 실패"는 실패가 아니라 **채택된 유동성 하한**이었고, 화면이 그 사유를 버리고 있었다.
근거: `docs/PREREG_R216_REBOUND_CONFIRMATION.md` · `_probe/r216_band_regime.py`
· `test_pipeline_fixes.py` §233
