# 클라우드 배포용 최소 의존성.
#
# ⚠️ easyocr / torch / paddleocr 는 **일부러 제외**했다.
#    Streamlit Community Cloud 는 컨테이너 메모리가 약 1GB 인데
#    torch 한 패키지만 2GB 를 넘어 배포 자체가 실패한다.
#
# 대신 온라인 스크린샷 인식은 Tesseract 로 돌린다.
#    엔진 본체(tesseract-ocr)와 한국어 데이터(tesseract-ocr-kor)는 apt 패키지라
#    packages.txt 에 있고, 아래 pytesseract 는 그 바인딩이다. 합쳐서 수십 MB 라
#    컨테이너 메모리 안에 들어간다. portfolio.ocr_backend() 는 pytesseract 를
#    먼저 찾으므로 클라우드에서는 이 경로로, 로컬에서는 easyocr 로 동작한다.
#    (인식 정확도는 easyocr 이 더 높다. 두 경로 모두 §33 교차검증을 통과해야
#     저장되므로, 오독은 미리보기에서 걸러진다.)

# 섹터 사이클(라운드 44) — 글로벌 섹터 ETF·환율·원자재·업종 분류 수신.
#
# ⚠️ **클라우드에서는 뺀다.** 라운드 44 배포에서 이 줄을 넣자마자 앱이
#    "Oh no. Error running app." 로 죽었다. 로컬(3.14)에서는 멀쩡하고,
#    FinanceDataReader 를 가려 놓고 임포트해도 전 모듈이 통과했다
#    (_probe/diag_cloud_boot.py). 즉 코드가 아니라 **설치 단계**가 문제다.
#
#    앱이 죽는 것보다 업황이 '미연동'으로 나가는 게 낫다 — sector_cycle 은
#    수신 실패를 available=False 로 내리고 화면이 그 사실을 적는다(§3).
#    클라우드 파이썬 버전에서 설치가 되는지 확인한 뒤 다시 넣는다.
#
# finance-datareader>=0.9.90     # 로컬에서만 (pip install finance-datareader)

streamlit>=1.40
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
Pillow>=10.0
openpyxl>=3.1
pytesseract>=0.3.10
