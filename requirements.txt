# 클라우드 배포용 최소 의존성.
#
# ⚠️ easyocr / torch / paddleocr 는 **일부러 제외**했다.
#    Streamlit Community Cloud 는 컨테이너 메모리가 약 1GB 인데
#    torch 한 패키지만 2GB 를 넘어 배포 자체가 실패한다.
#
# 대신 온라인 스크린샷 인식은 Tesseract 로 돌린다.
#    엔진 본체(tesseract-ocr)와 한국어 데이터(tesseract-ocr-kor)는 apt 패키지라
#    packages.txt 에 있고, 아래 pytesseract 는 그 바인딩이다. 합쳐서 수십 MB 라
#    컨테이너 메모리 안에 들어간다. portfolio.ocr_backend() 는 pytesseract 를
#    먼저 찾으므로 클라우드에서는 이 경로로, 로컬에서는 easyocr 로 동작한다.
#    (인식 정확도는 easyocr 이 더 높다. 두 경로 모두 §33 교차검증을 통과해야
#     저장되므로, 오독은 미리보기에서 걸러진다.)

# 섹터 사이클(라운드 44) — 글로벌 섹터 ETF·환율·원자재·업종 분류 수신.
#   pandas·requests·lxml 위에 얹히는 얇은 래퍼라 컨테이너 메모리에 부담이 없다.
#   없어도 앱은 뜬다 — sector_cycle 이 '미연동'으로 내려간다(§3).
#
#   ※ 배포가 죽었을 때 처음엔 이 줄을 범인으로 의심해 뺐다. 아니었다.
#     진짜 원인은 아래 starlette 였고, 빈 venv 재현으로 이 줄이 무죄임을
#     확인한 뒤 되돌렸다. **의심만으로 빼 두면 기능이 조용히 사라진다.**
finance-datareader>=0.9.90

streamlit>=1.40
# ⚠️ starlette 를 열어 두면 배포가 죽는다 (2026-08-05 실측).
#
#    streamlit 1.61.0 + starlette 1.4.0 조합에서 기동 즉시:
#      TypeError: GZipResponder.__init__() missing 1 required
#                 keyword-only argument: 'thread_minimum_size'
#    streamlit 의 SelectiveGZipMiddleware 가 starlette 의 GZipResponder 를
#    상속하는데, starlette 1.4 가 그 생성자에 필수 인자를 추가했다.
#
#    requirements 가 streamlit 만 열어 뒀고 starlette 는 아예 없어서,
#    **재빌드 때마다 최신 starlette 가 딸려 들어온다.** 코드를 한 줄도
#    안 고쳐도 어느 날 갑자기 앱이 죽는 구조였다.
#
#    클라우드와 같은 의존성만 담은 빈 venv 로 재현하고(_probe/cloudenv),
#    이 한 줄을 넣으니 streamlit 1.61.0 그대로 정상 기동을 확인했다.
starlette<1.4
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
Pillow>=10.0
openpyxl>=3.1
pytesseract>=0.3.10
