# -----------------------------------------------------------------------------
# AntiGravity AI Quant Simulator - Static Analysis Rulebook
# 이 파일은 quant_indicators.load_rulebook() 이 실제로 읽어 들이는 단일 출처다.
# 값을 바꾸면 엔진 산식이 그대로 따라간다. (구버전은 코드와 이 파일이 서로 달랐다)
# -----------------------------------------------------------------------------

[RULES_GENERAL]
version = "v2026.08.05"
currency_krx = "KRW"
currency_us = "USD"
currency_fx = "USD/KRW"

# 실행 가격 레벨 — 가상 백테스트 112회 진단으로 재설계 (2026-08-01)
# 진단: 목표/손절 비율 2.0 → 무추세 선도달확률 이론값 33% — 기하 자체가 적중률 상한.
# 처방(트레이더 관행): 손절은 변동성 2σ 바닥(노이즈 손절 방지, Wilder/Kaufman),
# 1차 목표는 '1차 분할익절'로 재정의해 손절거리×0.7 (무추세 이론 P≈59%),
# 구조적 저항 목표(기존 1차 로직)는 2차 목표가 담당한다.
[RULES_EXECUTION_LEVELS]
stop_vol_mult = 2.0
stop_floor_pct = 3.0
target1_of_stop = 0.7
target1_vol_floor_mult = 0.8
# 52주 저점권 매수 제한 — George–Hwang(2004) 52주 고점 모멘텀의 역면.
# 자체 리플레이 327건 실측: 저점권(<30%) 적중 57.1%(-5.5%p), 고점권 66.4%(+3.7%p).
range_low_pct = 30.0
range_low_cap = 64
# 월봉 10선 아래 = 추세 역행 (시계열 모멘텀, Moskowitz–Ooi–Pedersen 2012 계열).
# 리플레이 1,073건 실측: 10선 위 60.5% vs 아래 51.8% (-8.7%p, n=612/461).
m10_below_cap = 64

# 원시 종합점수 = 기본매력 * w_quality + 매매적합 * w_timing + 리스크 * w_risk
[RULES_TOP_LEVEL_WEIGHTS]
weight_stock_quality = 0.35
weight_trading_timing = 0.45
weight_risk_safety = 0.20

# 종목 기본 매력도 내부 가중치 (합 1.00)
[RULES_STOCK_QUALITY_WEIGHTS]
weight_fundamental = 0.30
weight_valuation = 0.25
weight_financial_safety = 0.20
weight_industry_competitiveness = 0.15
weight_long_term_structure = 0.10

# 현재 매매 적합도 내부 가중치 (합 1.00)
[RULES_TRADING_TIMING_WEIGHTS]
weight_historical_pattern = 0.22
weight_path_expected_value = 0.16
weight_demark_confluence = 0.14
weight_technical_structure = 0.13
weight_flow_volume = 0.11
weight_market_regime = 0.08
weight_probability_edge = 0.07
weight_price_location = 0.05
weight_event_timing = 0.04

# 리스크 안전성 내부 가중치 (합 1.00)
[RULES_RISK_SAFETY_WEIGHTS]
weight_reward_risk = 0.27
weight_volatility_mdd = 0.22
weight_financial_risk = 0.18
weight_event_risk = 0.13
weight_liquidity = 0.08
# 종목 뉴스에서 확인이 필요한 낱말이 잡힌 경우의 위험 (market_context 낱말 일치)
weight_news_risk = 0.12

# 시장 국면 점수 = 상장시장(코스피/코스닥) 국면 + 글로벌 위험 합성
# 한 쪽이 미수신이면 그쪽 가중치를 0으로 두고 나머지로 재정규화한다.
# 둘 다 미수신이면 점수를 만들지 않고 이 항목을 통째로 뺀다 (추측 금지).
[RULES_MARKET_CONTEXT]
weight_domestic_regime = 0.60
weight_global_risk = 0.40

# 국내 지수 국면별 점수
score_bull_strong = 78
score_bull_mild = 70
score_sideways = 55
score_bear_panic = 32

# 글로벌 위험 점수 = 100 에서 아래 감점을 차례로 뺀다 (하한 0)
global_base = 100
penalty_sp500_below_sma60 = 12
penalty_nasdaq_drawdown = 15
penalty_vix_warn = 10
penalty_vix_extreme = 25
penalty_fx_spike = 10
nasdaq_drawdown_pct = -10.0
vix_warn = 25.0
vix_extreme = 35.0
fx_spike_20d_pct = 3.0

# 뉴스 위험 점수 = 100 에서 위험 낱말 기사 1건당 감점 (하한 25)
# 기사 내용을 해석하지 않는다. 제목의 낱말 일치만 센다.
news_base = 100
news_penalty_per_article = 25
news_score_floor = 25
# 뉴스를 받지 못했을 때 쓰는 값. 벌점도 가점도 아닌 중립.
news_unavailable_score = 60

# 최종 행동 원점수 가중치 (합 1.00)
[RULES_FINAL_ACTION_WEIGHTS]
weight_confidence_adjusted = 0.45
weight_opportunity = 0.30
weight_execution = 0.15
weight_signal_consensus = 0.10

# 기회점수 내부 가중치 (합 1.00)
[RULES_OPPORTUNITY_WEIGHTS]
weight_expected_return = 0.30
weight_reward_risk = 0.20
weight_path_edge = 0.20
weight_historical_pattern = 0.15
weight_upside_room = 0.10
weight_liquidity = 0.05

# 실행가능성 내부 가중치 (합 1.00)
[RULES_EXECUTION_WEIGHTS]
weight_entry_range = 0.35
weight_chase_risk = 0.25
weight_liquidity = 0.20
weight_stop_structure = 0.10
weight_event = 0.10

# 분석 신뢰도 가중치 (합 1.00) — 명세 §14
[RULES_ANALYSIS_CONFIDENCE_WEIGHTS]
weight_data_confidence = 0.50
weight_stat_confidence = 0.30
weight_model_validation_confidence = 0.20

# 전략 품질 가중치 (Blind/OOS 구현 후 사용)
[RULES_STRATEGY_QUALITY_WEIGHTS]
weight_blind_oos = 0.20
weight_sharpe = 0.15
weight_sortino = 0.10
weight_mdd = 0.15
weight_net_return = 0.10
weight_turnover = 0.10
weight_regime_stability = 0.10
weight_alpha_persistence = 0.05
weight_benchmark_consistency = 0.05

# 경로 확률 판정 임계값 (%)
[RULES_PATH_THRESHOLDS]
take_profit_pct = 8.0
stop_loss_pct = 6.0

# 거래비용 가정 (%, 왕복)
[RULES_TRADING_COSTS]
commission_round_trip = 0.03
transaction_tax = 0.20
slippage = 0.18

# TOP3 추천 게이트 (명세 §15)
[RULES_TOP3_GATES]
min_action_score = 68
min_stock_quality = 60
min_trading_timing = 65
min_risk_safety = 55
min_opportunity = 65
min_execution = 60
min_expected_return_pct = 2.0
min_reward_risk = 1.3
min_effective_samples = 10
min_signal_consensus = 55

# 적정가 신뢰도 구간 (명세 §7)
[RULES_FAIR_VALUE_CONFIDENCE]
normal_display_min = 75
caution_min = 60
reference_only_min = 45
# 모델 적정가가 시장가격과 이 비율(%) 넘게 벌어지면 '저평가/고평가'가 아니라
# 모델이 이 종목의 가격을 설명하지 못하는 것이다(적용 범위 밖) — 숫자를 제시하지 않는다.
# 근거: Damodaran(고성장·스토리 기업은 이익·자산 멀티플이 성립하지 않음),
#       SR 11-7(모델을 설계 범위 밖에서 쓰는 것 자체가 모델 리스크).
out_of_domain_gap_pct = 70

# 유효표본 구간 (명세 §11)
[RULES_SAMPLE_TIERS]
insufficient_below = 5
observation_only_below = 10
low_confidence_below = 20
limited_below = 30

# 유동성 필터
[RULES_LIQUIDITY]
min_avg_turnover_krw = 2000000000
min_price_krw = 1000

# 자기유사 패턴 매칭 (명세 §5)
# Pearson 상관과 DTW 유사도를 결합해 판정한다. 코드에 박아두면 규칙집과 어긋난다.
[RULES_SIMILARITY]
pearson_weight = 0.7
dtw_weight = 0.3
dtw_scale_divisor = 3.0

# 데이터 무결성 게이트 (명세 §2)
# 출처 간 현재가 오차가 이 값을 넘으면 시세를 신뢰하지 않고 분석에서 제외한다.
[RULES_DATA_INTEGRITY]
price_cross_tolerance_pct = 1.0

# 탭별 관점 가중치 (참고용 — 종합 점수는 RULES_TOP_WEIGHTS 로 산출한다)
[RULES_TAB_WEIGHTS]
pattern = 0.25
valuation = 0.22
scenario = 0.15
demark = 0.13
technical = 0.13
integrity = 0.12

# -----------------------------------------------------------------------------
# 뉴스 규칙 (라운드 42 신설)
#
# 뉴스 엔진(news_feed.py)은 **재료를 재기만** 한다 — 신선도·방향·강도.
# 그것을 언제 가점·감점·차단으로 쓸지는 **여기서** 정한다. 역할이 다르다.
#
# ⚠️ 아래 숫자는 **설계값이지 실측값이 아니다.**
#    원장(virtual_graded.jsonl 19,883건)에 뉴스 필드가 없어 사후 검증을
#    못 한다. 그래서 이번 라운드에서는 **감점·차단만** 걸고 **가점은 걸지
#    않는다.** 좋은 뉴스로 점수를 올리는 것은 검증 없이 위험을 늘리는 쪽이고,
#    나쁜 뉴스로 막는 것은 검증 없이도 손실을 줄이는 쪽이기 때문이다.
#    (프로젝트 규칙 §2 — 숫자를 손으로 고르지 않는다. 검증될 때까지 비대칭)
#
# 케이스가 쌓여 뉴스 필드가 원장에 들어가면 사전등록 라운드로 재측정한다.
# -----------------------------------------------------------------------------
[RULES_NEWS]
# 신선 재료로 볼 시간 창 (시간)
fresh_window_hours = 24
# 위험 낱말이 붙은 신선 기사가 이 수 이상이면 신규 매수를 차단한다
# (상장폐지·거래정지·횡령·배임·분식·감사의견 거절 등)
risk_block_min_count = 1
# 위험 재료가 있을 때 씌우는 종합점수 상한
risk_score_cap = 55
# 위험 재료가 있을 때 씌우는 신뢰도 상한
risk_confidence_cap = 60
# 긍정 재료로 인한 가점 — **0 이다.** 검증 전까지 올리지 않는다
catalyst_score_bonus = 0
# 뉴스를 한 건도 못 받은 날은 게이트를 걸지 않는다 (미수신 ≠ 악재)
skip_gate_when_no_feed = 1
